' Demo-Programm zu REDIM
' (c) MAXON Computer 1993
'
' Erzeugt ein Feld mit 10 Buchstaben (jeder 2.
' Buchstabe von A bis S). Sucht danach zufÑllig
' einen Buchstaben aus dem Alphabet. Ist dieser
' Buchstabe schon im Feld, wird er dort gelîscht
' und das Feld verkleinert. Sonst wird er
' alphabetisch eingefÅgt und das Feld vergrîûert
' Beenden mit "Q"

OPTION BASE 1
DIM feld$(10)
FOR i%=1 TO 10
  feld$(i%)=CHR$(63+2*i%)
NEXT i%
REPEAT
  neu$=CHR$(65+RANDOM(26)) ! Neuer Buchstabe
  CLS
  PRINT "Arraygrîûe : ";DIM?(feld$());
        " EintrÑge        GewÑhlter Buchstabe : ";neu$
  PRINT
  FOR i%=1 TO DIM?(feld$()) ! Feld anzeigen (vor énderung)
    PRINT feld$(i%)'
  NEXT i%
  c%=INP(2)
  i%=1
  WHILE feld$(i%)<neu$ AND i%<DIM?(feld$()) !Hier werden
                              ! bis auf den letzten
    INC i%                    ! alle Buchstaben im Feld,
                              ! die kleiner sind als
  WEND                        ! der gewÑhlte Buchstabe,
                              ! Åbersprungen
  IF feld$(i%)=neu$           ! Sind die Buchstaben gleich
    DELETE feld$(i%)          ! Ja => Buchstabe lîschen
    redim(*feld$(),DIM?(feld$())-1,void%) ! Feld verkleinern
  ELSE                        ! Sonst...
    redim(*feld$(),DIM?(feld$())+1,void%) ! Erstmal Feld 
                                          ! vergrîûern
    IF feld$(i%)>neu$         ! Ist der gewÑhlte Buchstabe kleiner?
      INSERT feld$(i%)=neu$   ! Wenn ja => vor dem aktuellen im Feld einfÅgen
    ELSE                      ! Ansonsten ist der gewÑhlte grîûer als alle
      INSERT feld$(i%+1)=neu$ ! im Feld => ganz hinten eintragen
    ENDIF                     ! ( i% ist dann automatisch der letzte Feldbuchstabe)
  ENDIF
UNTIL c%=81 OR c%=113         ! Wartet auf Taste "Q"
'
' Hier nun noch REDIM hinzuMERGEn !!!
