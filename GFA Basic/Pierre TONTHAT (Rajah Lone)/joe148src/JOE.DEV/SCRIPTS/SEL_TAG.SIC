// Select all the same tags (
// tested with SCRIPTER Demo

// RENAME THE FILE !!! 
// OPEN JOE'S MAIN WINDOW TO SEE THE SELECTIONS

joe.open("G:\\TEST2.HTM");

// select the images
okay = joe.selectFirstTag("IMG", "tag.element");
while (okay) {
  /* copy/cut/paste/what_you_want there */
  okay = joe.selectNextTag();  
  }

// select the font group
okay = joe.selectFirstTag("FONT", "tag.group");
while (okay) {
  /* copy/cut/paste/what_you_want there */
  okay = joe.selectNextTag();  
  }

// select the common links
okay = joe.selectFirstTag("A", "tag.group");
while (okay) {
  /* copy/cut/paste/what_you_want there */
  okay = joe.selectNextTag();  
  }

// select the tables
okay = joe.selectFirstTag("TABLE", "tag.group");
while (okay) {
  /* copy/cut/paste/what_you_want there */
  okay = joe.selectNextTag();  
  }

joe.deselect();