RESERVE 2000
~XBIOS(5,L:HIMEM,L:HIMEM,TRUE)
FOR i=0 TO 24
  PRINT i;" 274 lignes a l ecran en GFA..."
NEXT i
BMOVE XBIOS(2),XBIOS(2)+32000,11840
REPEAT
  VSYNC
  ~TAN(0)
  SPOKE &HFF820A,0
  ~SINQ(0)
  SPOKE &HFF820A,2
  FOR i=0 TO 600
  NEXT i
  SPOKE &HFF820A,0
  ~SINQ(0)
  SPOKE &HFF820A,2
UNTIL INP(-2)
