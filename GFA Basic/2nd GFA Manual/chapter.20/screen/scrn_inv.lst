' *** SCRN_INV.LST ***                               2nd GFA-Manual, page 20-29
> PROCEDURE screen_invert(invert!)
  '
  ' *** Invert High resolution screen if flag!=TRUE
  ' *** Normal screen if flag!=FALSE
  '
  IF invert!
    VSETCOLOR 0,0               ! invert (white letters on black background)
  ELSE
    VSETCOLOR 1,0               ! normal (black letters on white background)
  ENDIF
RETURN
' *********
'
