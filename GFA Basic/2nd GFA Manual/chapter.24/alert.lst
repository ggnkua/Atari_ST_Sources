' *** ALERT.LST ***                                   2nd GFA-Manual, page 24-4
> PROCEDURE alert(symb,text$,default,button$,VAR choice)
  '
  ' *** Use this Procedure with the same parameters/syntax as the ALERT-command
  ' *** Instead of 4, you can now use 5 lines in text$
  '
  choice=FORM_ALERT(default,"["+STR$(symb)+"]["+text$+"]["+button$+"]")
RETURN
' *********
'
