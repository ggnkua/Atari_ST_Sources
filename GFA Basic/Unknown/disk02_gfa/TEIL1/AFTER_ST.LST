' AFTER STOP Beispiel:
'
t%=TIMER
AFTER 600 GOSUB proc1x
PRINT "AFTER 600 GOSUB ist aktiv"
EVERY 400 GOSUB proc2x
DO
LOOP
PROCEDURE proc1x
PRINT "AFTER 600 GOSUB nach ";(TIMER-t%);" Ticks ausgefÅhrt!"
EDIT
RETURN
PROCEDURE proc2x
a%=a% XOR 1
IF a%=1
PRINT "AFTER 600 GOSUB unterbrochen"
AFTER STOP
ELSE
PRINT "AFTER 600 GOSUB wieder aufgenommen"
AFTER  CONT
ENDIF
RETURN
