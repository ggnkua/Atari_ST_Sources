/*
  Listing 1
  fÅr Turbo C 2.0
  Autor: T.W. MÅller
  (C) by Maxon Computer GmbH, 1994
*/

#include <stdio.h>
#define Nmax 3

main()
{
  int Vektor[]={1,2,3};
  int i;

  for (i=0;i<Nmax;i++)
    printf("\nVektor[%d] = %d\n",i,Vektor[i]);

  getchar();

  /* Sonst meckert TC.PRG nach Programmende. */
  return 0;
}

