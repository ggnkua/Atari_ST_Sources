LOUIS VINCENT                                       22.11.86
8 RUE DU BROCHET
F-67300 SCHILTIGHEIM


                       3 D  ZEICHNUNGS PROGRAMM
                       ------------------------

  DIESES PROGRAMM DIENT ZUR ERSTELLUNG VON PERSPEKTIVEN.

  DIE PUNKTE (POLYLINE) WERDEN MIT HILFE VON 3 'FENSTERN'
  ERFASST:
                      MOUSE KNOPF LINKS        RECHTS

    OBEN-LINKS  :            X                   Y
    OBEN-RECHTS :            Z                   Y
    UNTEN-RECHTS:            Z                   X

    UNTEN-LINKS : RESERVIERT FUER DIE PERSPEKTIVE. DIE PERSP KANN
                  IHREN RAHMEN UEBERSCHREITEN. AUF GRUND DER POSITION
                  DES BEOBACHTERS (XV,YV,ZV) UND DEN BEOBACHTUNGS
                  WINKELN (AL,BE,GA,PH  (PH IST DIE OEFFNUNGS 'ZOOM')).

    +     :  SETZEN PUNKT (MIT AKTUELLEN X,Y,Z KOORDINATEN)
    -     :  LETZER PUNKT LOESCHEN
    *     :  ENDE 'POLYLINE' (ANFANG NEUE )

    H     :  EIN DEFINIERTES VIELECK 'POLYGON' ERHOEHEN (Z AXE).
             DER POLYGON WIRD NOCHEINMAL GEZEICHNET: UM DEN
             VORGEGEBENEN DZ WERT ERHOEHT.
             DIE KANNTEN WERDEN GEZEICHNET: (X,Y,Z),(X,Y,Z+DZ)
                                            (X',Y',Z'),(X',Y',Z'+DZ)
                                             ETC.....

    MANCHE 'MENU' FUNKTIONEN KOENNEN AUCH UEBER DEN
    BUCHSTABEN DES MENUS MIT DER TASTATUR EINGEGEBEN WERDEN.

    FUNKTIONEN DIE NICHT IM MENU SIND :

    P     : HARDCOPY (MASSGERECHT): HARCOPY.PRG WIRD GELADEN.
            FUNKTIONIERT (BEI MIR) LEIDER NUR MIT DEM GFA.BASIC
            NICH MIT  GFA.BASRO.

    C     : BILDSCHIRM WIRD NEU GEZEICHNET.

    F     : FULLEN MIT DER GEWAEHLTEN FULLFARBE (OPTIONS).
            (MANUELLE SCHATIERUNG)

    DIE KOORDINATEN WERDEN IM UNTER-ORDNER \CRD\ GESPEICHERT.
    (ASCII ) SIE KOENNEN MIT EINEM EDITOR GEAENDERT WERDEN !!!!.

    DIE BEOBACHTER POSITION KANN GELADEN BZW GESPEICHERT
    WERDEN. (UNTER-ORDNER \PO\).

    DAS HARDCOPY.PRG MUSS IM GLEICHEN ORDNER ALS DAS ZEICHNUNGS
    PROGRAMM SEIN. DAS ZEICHNUNGS PROGRAMM MUSS DIREKT GESTARTET
    WERDEN. (NICHT MANUEL DEN GFA.BASIC VORLADEN WENN ER NICHT IM
    GLEICHEN ORDNER IST, SONST UNBERGIBT GFA NICHT DEN GEWUNSCHTEN
    AKTUELLE ORDNER.)

    GEGEN EINSENDUNG EINER DISKETTE UND 30 DM  SENDE ICH IHNEN
    GERN DAS KOMMENTIERTE SOURCE PROGRAMM ZU.

    M.F.G.

    V.LOUIS

