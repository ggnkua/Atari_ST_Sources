latt
> PROCEDURE latt
  INPUT "plus eller minus.Tryck + eller - ";satt$
  IF satt$="+"
    GOSUB pluslatt
  ENDIF
  IF satt$="-"
    @minuslatt
  ENDIF
RETURN
> PROCEDURE pluslatt
  CLS
RETURN
> PROCEDURE minuslatt
  CLS
RETURN
