PROCEDURE v_pline(handle%,count%,pxyarray)
  ' polyline
  '   Polygonzug wird gezeichnet
  '   Ein einzelner Punkt kann durch die Linie von einem Punkt zu sich
  '   selbst dargestellt werden
  @pxyarray(pxyarray,count%)
  @vin(6,count%,0,handle%)
RETURN
