PROCEDURE v_meta_extents(handle%,pxyarray)
  ' update metafile extents
  '   die Gr”en-Infos des Metatreibers werden neu gesetzt
  ' fr METAFILE
  DPOKE CONTRL+2,2
  @pxyarray(pxyarray,2)
  @ein(98,handle%)
RETURN
