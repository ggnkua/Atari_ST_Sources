PROCEDURE vq_curaddress(handle%,row%,column%)
  ' inquire current alpha text cursor address
  '   erfragt aktuelle Cursorposition
  ' fÅr BILDSCHIRM
  DPOKE CONTRL+8,2
  @ein(15,handle%)
  *row%=DPEEK(INTOUT)
  *column%=DPEEK(INTOUT+2)
RETURN
