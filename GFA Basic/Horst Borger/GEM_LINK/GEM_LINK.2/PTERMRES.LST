DEFFN ptermres(keep%,ret%)=GEMDOS(49,keep%,ret%)
' terminate and stay resident
' das laufendde Programm wird abgebrochn. Allerdings kann dabei eine
' bestimmte Anzahl von Bytes resident im Speicher gehalten werden
' z.B. bei Treibern fr RAMdisks
'     keep%
' Anzahl der resident zu haltenden Bytes ( ab ANFANG Basepage )
'     ret%
' Status, der an das aufrufende Programm zurckgegeben wird
' 0=OK, GEMDOS-Fehlernummern, interne Nummer (z.B. Adressen !)
'
