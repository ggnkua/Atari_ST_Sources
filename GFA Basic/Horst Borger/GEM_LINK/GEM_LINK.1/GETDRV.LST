PROCEDURE getdrv(drv%)
  ' ermittelt, ob ein Laufwerk angeschlossen ist
  IF 2^drv% AND @gemdos(14,-1)
    ' es wird nachgeschaut, ob das Bit des Laufwerks gesetzt ist
    drv_set!=TRUE
  ELSE
    drv_set!=FALSE
  ENDIF
RETURN
