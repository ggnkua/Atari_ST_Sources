' *** MOUSE.LST ***                                   2nd GFA-Manual, page 14-6
> PROCEDURE mouse(switch!)
  '
  ' *** Disable (switch!=FALSE) or activate (TRUE) the mouse
  '
  IF switch!
    OUT 4,8                     ! activate mouse again
  ELSE
    OUT 4,18                    ! disable mouse temporarily
  ENDIF
RETURN
' *********
'
