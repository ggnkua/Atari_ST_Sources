
----------------
MADMAC TODO LIST
----------------


FEATURES
	.list/.nlist sym, mac, con, include
	flush local symbols in symbol listing
		actually, do a sub-sort "behind" the main symbol...
	labels on .rept .irp .irpc
	.sys trapno,args[.size]...
	.ccall ea, args[.size]...
	.pcall ea, args[.size]...			no cleanup
		thing:  move thing,-(sp)
		#thing: move #<thing>,-(sp)
		&thing: pea <thing>
		special-case 0?
	push current section / pop current section
	speed up getln() and tokln() [in assembly on the ST?]
	macro argument shifting
		.shift [n]
	unary / (bits 8..15)
	unary & (bits 0..7)
	summarize undefined-but-referenced symbols
	remove (or append to) file specified with "-e"
	.opt undef, EQ to saying "-U" on commandline
	@"string" deposits string in data, yeilds addr of string
	clean up procln()
	'~' and back-quote become alpha characters in -6 mode

BUGS
