Version 1.40:
    - neue Funktionen:
        int appl_xgetinfo(int type,int *out1,int *out2,int *out3,int *out4)
          => erweiterte appl_getinfo()-Routine von Martin Osieka @ OF2, die
             selbstÑndig die Existenz der appl_getinfo()-Routine ermittelt
        void v_set_mode(int mode)
        void v_set_text(int font,int height,int color,int *out)
        void v_set_line(int color,int width)
        void v_set_fill(int color,int inter,int style)
          => Einstellen der Attribute der internen VDI-Workstation 'x_handle'
          => negative Parameter Ñndern die Einstellung nicht
          => interne, optimierte Verwaltung der eingestellten Attribute
          => v_set_text:
               height<0: Angabe in Punkt
               height>0: Angabe in Pixel
               out: Zeiger auf Integerfeld mit 4 Elementen oder NULL
                 -> Ausgabeparameter von vst_height/vst_point
          => alle Attribute, die nicht Åber obige Funktionen verÑndert werden
             kînnen, dÅrfen nur kurzfristig verÑndert werden
        void vs_attr(void)
          => wurden die Attribute der internen VDI-Workstation 'x_handle'
             nicht Åber obige Funktionen verÑndert, so muû diese Funktion
             sofort danach aufgerufen werden, um dies der Library kenntlich
             zu machen
    - DEBUG-Modus (Fehlermeldungen bei falschem Resource-Aufbau) aktiv:
        -> bei doppelten Hotkeys werden jetzt ebenfalls Warnungen ausgegeben
    - fehlerhafte Eintragung als Accessory unter Mag!X 2.0 korrigiert
    - Ermittlung der Grîûe des System-Fonts verbessert
    - Anzahl der VDI-Aufrufe drastisch reduziert
        -> schnellere Ausgabe von erweiterten Objekten
    - kleinere Fehler in der Anpassung an GNU und Lattice korrigiert
    - bei aktiven fliegenden Dialogen werden keine Nachrichten fÅr geîffnete
      Fensterdialoge mehr ausgewertet
    - modale Fensterdialoge kînnen jetzt ebenfalls ikonifiziert werden
    - fehlerhafter shel_write()-Aufruf unter Mag!X fÅhrte zu Fehlermeldungen
      am Programmende
    - MenÅleiste wurde unter MTOS teilweise nach einem Prozeûwechsel nicht
      mehr korrekt dargestellt (Funktion menu_enable())
    - Alert-Boxen sind wieder - sofern mîglich - applikations-modal
    - interne Initialisierung konnte in seltenen FÑllen, u.a. teilweise
      beim Schlieûen/Wechseln von Fenstern Åber einen Tastendruck, abstÅrzen

Version 1.41: (nicht îffentlich)
    - neue Funktion:
        int wind_xget(int w_handle,int w_field,int *p1,int *p2,int *p3,int *p4)
            => korrigierte wind_get-Version fÅr erweiterte Aufrufe unter Winx
               sowie MTOS
            => Winx-Erkennung funktionierte mit der optimierten wind_get()-
               Funktion der Pure-C-Library nicht bzw. nicht zuverlÑssig
    - Rechtsklick auf ein Objekt im Vordergrunddialog simuliert Doppelklick
    - Anpassung an Window-Shading von Winx 2.3
    - teilweise fehlerhaftes Event-Handling nach einer modalen Dialogbox (z.B.
      einer Alertbox) korrigiert
    - Popup()-Funktion funktioniert jetzt korrekt, wenn in der Åberge-
      benen POPUP-Struktur der Eintrag 'p_info' oder in der DIAINFO-
      Struktur der Eintrag 'di_tree' auf NULL gesetzt ist
    - Sliderscrolling im Hintergrund funktionierte im Beispielprogramm nicht
      korrekt (Clipping)
    - es kînnen jetzt mehrere USERFLY-Objekte in einem Dialog vorkommen
    - Probleme mit USERFLY-Objekten in Fensterdialogen behoben

Version 1.45:
    - neue Funktionen:
        - void vsf_aespattern(int handle, int ob_x, int ob_y, int pattern)
            => von Martin Osieka @ OF2
            => berechnet (und setzt) aus der Objektposition (ob_x,ob_y) das
               AES-Muster 'pattern' (0-7) positionsabhÑngig, so daû die AES-
               Muster positionsunabhÑngig aussehen
        - void v_aespattern(int ob_x, int ob_y, int pattern)
            => analoge Funktion wie vsf_aespattern fÅr die Library-interne
               VDI-Workstation 'x_handle'
    - neue Parameter:
        - void graf_rt_slidebox(SLINFO *,int,int double_click)
            => Ein Wert ungleich Null bedeutet, daû das Objekt 'obj' mit
               einem Doppelklick angewÑhlt wurde
        - void rsrc_init(int,int,int,int,int,char **,long *,BITBLK *,
               long *,ICONBLK *,TEDINFO *,OBJECT *,OBJECT **,RS_IMDOPE *,
               int orig_cw,int orig_ch)
        - void rsrc_calc(OBJECT *,int,int orig_cw,int orig_ch)
            => Grîûe des Fonts, mit dem die Dialoge entworfen wurden, so daû
               die Dialoge besser an die Grîûe des System-Fonts angepaût
               werden kînnen
               (Anpassung wie bisher bei orig_cw<=1 oder orig_ch<=1)
    - bei einem Doppelklick auf die Slider-Pfeile wird an den Anfang
      bzw. das Ende des Sliders gesprungen
    - Zugriff auf Tastatur-Tabelle wegen Speicherschutz im Supervisor-Modus

Version 1.46: (nicht îffentlich)
    - neue Variable 'mint' (int):
        => MiNT-Versionsnummer oder 0
    - History-/Undo-/Clipboard-Funktionen beachten beim EinfÅgen die GÅltig-
      keitsmaske eines Eingabefeldes
    - Undo-Funktion in Eingabefeldern Åber Shift-Undo anwÑhlbar, sofern ein
      Undo-Button vorhanden ist
    - alle internen globalen Variablen-/Funktionsnamen beginnen mit einem
      Unterstrich '_'
    - in der Eingabebox fÅr ASCII-Zeichen waren zwei Zeilen vertauscht
      und das Zeichen '@' nicht anwÑhlbar
    - neues Checkbox-Image: 'HÑkchen-Box' check_image(5,NULL)
    - neue Parameter:
        - void scale_image(OBJECT *,int, int orig_cw,int orig_ch)
        - void fix_objects(OBJECT *,int, int orig_cw,int orig_ch)
            => Grîûe des Fonts, mit dem die Dialoge entworfen wurden, so daû
               die Dialoge besser an die Grîûe des System-Fonts angepaût
               werden kînnen
               (Anpassung wie bisher bei orig_cw<=1 oder orig_ch<=1)

Version 1.50: (nicht îffentlich)
    - Verwaltung des Text-Cursors sowie der Eingabefelder komplett neu und
      unabhÑngig von den entsprechenden AES-Routinen programmiert
        -> kleiner/groûer System-Font kînnen in Eingabefeldern benutzt
           werden
        -> Cursor und Undo-Funktion sind jetzt in jedem Dialog vorhanden
           (bisher nur im aktiven Dialog)
        -> in Eingabefeldern fÅr Pfad/Dateinamen kînnen nur die zulÑssigen
           Zeichen eines GEMDOS-Dateinamens eingegeben werden
        -> grîûtenteils automatische Anpassung (sofern mîglich) der
           eingegeben Zeichen an GÅltigkeitsmaske
        -> neue zusÑtzliche GÅltigkeitsmasken:
            'c','C': Ziffern "0-9", Komma ".,", Rechenzeichen "+-*/^",
                     Klammern "(){}[]"
            'l','L': LED-Ziffern "0-9"
            '<Ziffer>': alle Ziffern von '0' bis <Ziffer>
            'h','H': Hexadezimalziffern "0-9A-F",
                     Umwandlung in Klein/Groûbuchstaben
            'u','U': ASCII 32-127, Umwandlung in Klein-/Groûbuchstaben
            'v','V': ASCII 32-255, Umwandlung in Klein-/Groûbuchstaben
            'w','W': ASCII 32-127
            'y','Y': ASCII 32-255
    - neue Mîglichkeiten in Eingabefeldern:
        -> Sonderzeicheneingabe durch Eingabe des dezimalen ASCII-Wertes
           bei gedrÅckter Alternate-Taste auf dem Ziffernblock
             => sofern der ASCII-Wert nicht eindeutig ist, mÅssen alle
                drei Stellen eingegeben werden
        -> Insert schaltet zwischen EinfÅge- und öberschreiben-Modus um
           (Unterscheidung durch Strich-/Block-Cursor)
        -> Shift-Backspace lîscht Eingabefeld bis Cursor
    - neue Parameter:
        -> boolean open_dialog(OBJECT *,DIAINFO *,char *,boolean,boolean,int,
           int edit)
            => Objektnummer des Eingabefelds, in welchem sich der Cursor
               befinden soll, oder negativer Wert (ehemaliger Parameter der
               Funktion X_Form_Do())

Version 2.00:
    - Quelltexte nahezu komplett Åberarbeitet
    - Beispiel Åberarbeitet, korrigiert und erweitert
    - Definitionen von
        -> Clipboard-Protokoll (SC_CHANGED)
        -> MenÅ-Protokoll
        -> XAcc-2-Protokoll
        -> AV-Protokoll
      in Header-File (E_GEM.H) integriert)
    - Funktionen, Variablen, Definitionen und Strukturen dokumentiert (auch als
      Hypertext vorhanden!)
    - XAcc2- und AV-Protokoll
        -> alle An- und Abmeldungen (inkl. der Fenster beim AV-Server) sowie
           AV_SENDKEY/ACC_KEY werden automatisch verwaltet
        WICHTIG: Die AES-Funktionen evnt_multi(), EvntMulti (Pure C) sowie
        evnt_mesag() dÅrfen nicht mehr benutzt werden! Stattdessen MUû die
        Funktion Event_Multi benutzt werden!
    - neue Struktur XAcc:
        int flag:      Bit 0: Xacc-Protokoll
                       Bit 1: AV-Protokoll
        int id:        Applikations-ID
        int version:   Highbyte: Programmversionsnummer
                       Lowbyte:  unterstÅtzte Nachrichtengruppen
        int menu_id:   ID des MenÅeintrags im Accessory-MenÅ
        char name[10]: AV-Programmname fÅr appl_find
        char *xname:   (erweiterter) XAcc2-Programmname oder Null
        char *xdsc:    Zeiger auf 'XDSC'-Informationen oder Null
    - neue Struktur WIN fÅr Fenster (s. Anleitung)
    - Ereignis-Verwaltung erweitert (mehrere Rechteck-, Button- und Timer-
      Events, s. Dokumentation der XEVENT-Struktur)
    - neue Funktionen:
        void XAccBroadCast(int msg[8])
            -> Nachricht 'msg' an alle erreichbaren Applikationen verschicken
        int XAccSendAck(int sendto, int answer)
            -> ACC_ACK-RÅckmeldung 'answer' (answer==0 -> Fehler) an Appli-
               kation 'sendto' senden
        int XAccSendKey(int sendto, int scan, int state)
            -> Tasten-Druck (Scancode 'scan',Status der Umschalttasten
               'state') an Applikation 'sendto' senden
        int XAccSendText(int sendto, char *txt)
            -> Text 'txt' an Applikation 'sendto' senden
        int XAccSendMeta(int sendto, int last, char *data, long len)
        int XAccSendImg(int sendto, int last, char *data, long len)
            -> Metafile-/GEM-Image-Block 'data' der LÑnge 'len' an Appli-
               kation 'sendto' senden
            -> last!=0 -> letzter Datenblock
        int AvSendMsg(int sendto,int msg_id,int msg[8])
            -> (AV-) Nachricht 'msg_id' an Applikation 'sendto' senden
            -> msg[3]-msg[7] enthalten gegebenenfalls weitere Daten
        => RÅckgabewert: TRUE -> OK, FALSE -> Fehler, FAIL -> TIMEOUT
        XAcc *find_xacc_xdsc(int app,char *dsc)
            -> erweiterten XAcc-Programmnamen der Applikation app nach
               String dsc durchsuchen (app<0 -> alle Applikationen)
        XAcc *find_id(int app)
            -> XAcc-Struktur der Applikation app ermitteln
        XAcc *find_app(int first)
            -> XAcc-Strukturen der angemeldeten Applikationen ermitteln
            -> first!=0 -> erste (ansonsten nÑchste) Applikation
        => RÅckgabewert==Null, wenn keine entsprechende XAcc-Applikation
           gefunden werden konnte
        int AppLoaded(char *name)
            -> Ermittelt die Applikations-ID der Anwendung mit dem
               Dateinamen 'name', welcher auch Pfad- und Laufwerksangaben
               enthalten darf.
            -> RÅckgabewert: Applikations-ID oder negativer Wert
        void scrp_changed(int format,long best_ext)
            -> Clipboard-Inhalt wurde verÑndert (SC_CHANGED-Nachricht
               wird (per XAccBroadCast) an alle Applikationen gesendet)
            -> format:   Bitmap des Dateiformats
               SCF_INDEF  = $0000;
               SCF_DBASE  = $0001 Daten, die in eine Datenbank geladen
                                  werden kînnen (".DBF", ".CSV", ...)
               SCF_TEXT   = $0002 Textdateien (".TXT", ".ASC", ".RTF" ...)
               SCF_VECTOR = $0004 Vektorgrafik (".GEM", ".EPS", ...)
               SCF_RASTER = $0008 Rastergrafik  (".IMG", ".GIF", ...)
               SCF_SHEET  = $0010 Tabellenkalkulation (".DIF", ".WKS", ...)
               SCF_SOUND  = $0020 Samples, MIDI-Files, KlÑnge (".MOD" ...)
            -> best_ext: 4 Zeichen (z.B. ".RTF") fÅr die "beste" der
                         abgespeicherten Dateien
        int get_dialog_info(int *dials,int *wins,DIAINFO **top)
            -> Informationen Åber Dialoge ermitteln (s. Dokumentation)
        void Event_Handler(int (*foo1)(XEVENT *,int),int (*foo2)(XEVENT *));
            -> Routinen zur Ereignisauswertung festlegen (ehemalige Para-
               meter der Funktionen X_Form_Do(), xdialog(), xalert() und
               ascii_box())
        void Event_Timer(int locount,int hicount)
            -> Wartet bis die durch lo_count und hi_count in Millisekunden
               vorgebene Zeit verstrichen ist, wobei die Library weiterhin
               anderer Ereignisse automatisch auswertet
        WIN *open_window(char *title,char *info,int typ,int box,int x,int y,
                        int w,int h)
            -> Fenster mit den Ausmaûen (x,y,w,h) und den Elementen 'typ'
               sowie dem Titel 'title' (oder NULL) und der Info-Zeile
               'info' (oder NULL) îffnen und gegebenenfalls (box!=0) sich
               vergrîûernde Rechtecke zeichnen
            -> RÅckgabewert: Zeiger auf WIN-Struktur oder NULL
        int close_window(WIN *window,int box)
            -> Fenster 'window' schlieûen und gegebenenfalls (box!=0) sich
               verkleinernde Rechtecke zeichnen
            -> RÅckgabewert: TRUE -> Okay, FALSE -> Fehler
        WIN *get_window(int handle)
            -> WIN-Struktur zum Fenster 'handle' ermitteln
            -> RÅckgabewert gleich NULL, falls Struktur nicht gefunden
               werden konnte
        void CycleCloseWindows(char cycle_hot,char close_hot,int cycle_menu,
                               int close_menu)
            -> Hotkeys und MenÅpunkte fÅr Fenster wechseln/schlieûen
            -> cycle_hot,close_hot:   Hotkey-Zeichen (meistens 'W' bzw. 'U')
            -> cycle_menu,close_menu: Objektnummern der MenÅeintrÑge oder
                                      negativer Wert
        int menu_install(OBJECT *menu,int show);
            -> MenÅ-Leiste 'menu' darstellen (show!=0) oder lîschen
               (show==0). Diese Funktion berÅcksichtigt, ob gegebenenfalls
               auch ein Accessory eine MenÅ-Leiste besitzen darf und ini-
               tialisiert die MenÅ-Punkte 'Fenster wechseln' und 'Fenster
               schlieûen'
            -> RÅckgabewert wie bei 'menu_bar'
    - neue Parameter:
        boolean open_rsc(char *,char *,char *xacc_name,char *av_name,
                         int av_msgs,int va_msgs,int xacc_msgs)
        boolean init_gem(char *,char *xacc_name,char *av_name,
                         int av_msgs,int va_msgs,int xacc_msgs)
            x_name/a_name:   (erweiterter) XAcc2-/AV-Programmname
            av_msgs/va_msgs: Bitvektor der unterstÅtzten AV/VA-Nachrichten
                             fÅr AV_PROTOCOL (MSG_SETSTATUS, MSG_START) und
                             VA_PROTOSTATUS (MSG_ASKFILEFONT-MSG_PATH_UPDATE)
            xacc_msgs:       Bitvektor der ACC-Nachrichten, die ausgewertet
                             werden (X_MSG_TEXT, X_MSG_META, X_MSG_IMG)
        void dial_options(boolean,boolean,boolean,boolean,boolean,boolean,
                          boolean,boolean,boolean toMouse);
            -> TastendrÅcke an Fenster unter Mauszeiger senden (toMouse!=0)
        int xalert(int,int,int,BITBLK *,int center,int widthchar *,char *,char *);
            -> center: -> Buttons ausrichten
               BUTTONS_CENTERED: zentriert,
               BUTTONS_LEFT:     linksbÅndig,
               BUTTONS_RIGHT:    rechtsbÅndig
            -> width:  -> einheitliche Breite der Buttons (width!=0)
        boolean open_dialog(OBJECT *,DIAINFO *,char *,boolean,boolean,int,
                            int, SLINFO **slider)
            -> Zeiger auf Liste mit Adressen der SLINFO-Strukturen des
               Dialogs oder NULL, wenn die Slider manuell verwaltet werden
            -> SLINFO-Struktur wurde erweitert, so daû die Slider per
               Tastatur bedient werden kînnen (s. Dokumentation, u.a.
               SLKEY-Struktur)
    - neue globale Variablen:
        (int) 'multi':
          -> MultiTasking-Betriebssystem vorhanden (multi!=0)
        (int) 'AvServer':
          -> Applikations-ID des AV-Servers (AvServer>=0)
        (int) 'menu_available':
          -> Anwendung darf eigene Drop-Down-MenÅleiste anmelden
             (menu_available!=0)
    - neuer erweiterter Objekt-Typ:
        G_UNMODAL (0x4000, Bit 14 im Objekt-Typ):
            -> Objekt kommt nur in unmodalen Dialogen vor (analog zum
               Typ G_MODAL), wobei dieser Status bei jedem Objekttyp
               gesetzt werden kann
    - SC_CHANGED-Nachricht wird automatisch versendet, wenn sich der
      Clipboard-Inhalt durch eine Bibliotheksfunktion geÑndert hat
    - an zukÅnftige Erweiterungen und Geneva durch Nutzung von appl_xgetinfo
      angepaût (u.a. MenÅleisten als Accessory unter MagiC)
    - alle Fenster (nicht nur Fensterdialoge) kînnen jetzt unter allen
      TOS-Versionen im Hintergrund bedient werden
    - ICFS wird unterstÅtzt sowie automatische (De-) Ikonifizierung aller
      Fenster
    - bei vorhandenem ICFS-Server kînnen Fenster per Ctrl-'*'
      (Ziffernblock) ikonifiziert werden
    - die Ereignis-Auswertungsroutine, welche an die Funktionen
      open_dialog(), xalert(), xdialog() und ascii_box() Åbergeben wird,
      muû jetzt zurÅckliefern, welche Ereignisse ausgewertet wurden
    - Nachricht OBJC_CHANGED wird an Applikation geschickt, wenn der Status
      eines Dialog-Objekts geÑndert wurde, sowie Nachricht OBJC_EDITED
      erweitert (s.a. Dokumentation der Funktion 'Event_Handler')
    - Nachricht WIN_CHANGED wird an Applikation geschickt, wenn sich der
      Status oder das Handle eines Fensters geÑndert hat (Fenster wurde z.B.
      (de-) ikonifiziert)
    - Nachrichten XACC_AV_INIT/EXIT werden an Applikation geschickt, wenn
      eine Applikation sich per XAcc-2 oder AV an- bzw. abgemeldet hat
      (s.a. Dokumentation der Funktion 'Event_Handler')
    - die Routinen 'redraw_dialog' und 'move_dialog' wurden entfernt, da
      Event_Multi jetzt diese Aufgaben automatisiert
    - globale Variable 'dinfo' entfernt
    - Skalierung von Images/Icons bei unterschiedlichem System-Font
      verbessert
    - Rechtsklick auf ein Objekt im Hintergrunddialog simuliert Doppelklick,
      sofern Hintergrundbedienung aktiv ist
    - Fenster schlieûen/wechseln (Ctrl-W/Ctrl-U) integriert
    - Klick auf deaktivierte MenÅ-Leiste fÅhrte aufgrund eines AES-Fehlers
      zu einem System-HÑnger