Die Datei Crystal.o enth„lt die Definitionen fr die Dateien f_button.c und
f_keybd.c! Sie mu mit dem Kommando 'ar rk libaes.a crystal.o' in die
AES-Bibliothek des MWC eingebunden werden. Sonst laufen die genannten
Funktionen nicht. Die Datei exformdo.c enth„lt eine neue Form_do()-Routine,
die es erm”glicht, Dialogboxen mit der Tastatur zu steuern. (genaueres
bitte der Anleitung entnehmen!). Datei bersetzen und ebenfalls der
libaes.a zufgen (alte Form-do-Routine bleibt bestehen, neue wird mit dem
Namen exform_do() aufgerufen!). Exform_do() ben”tigt f_keybd.c und
f_button.c zur Laufzeit (sind undokumentierte Funktionen des AES! - selbst
die Form_do()-Routine macht ausgiebig gebrauch von diesen!).
Bei Problemen k”nnen die ver„nderten Bibliotheken bei
Michael Kovar, Waldmannstr. 17, 1000 Berlin 46
bezogen werden.


