# MINT:
# just a first try for the ST Terminals. Look into the other files
# to get ideas on what could be done here, too.

# Arrow keys
bind-to-key backward-character ^[D
bind-to-key previous-line ^[A
bind-to-key next-line ^[B
bind-to-key forward-character ^[C


# The vt100 is the only terminal that needs flow control to throttle
# output from the computer. The following line is unlikely to be needed
# for any vt100-emulator. Mint: first I don't set it off:
#set allow-^S-and-^Q off
