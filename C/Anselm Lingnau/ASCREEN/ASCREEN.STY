% DIN-A4-Gr"osse
\hoffset=0pt
\voffset=0pt
\textheight=594.8 pt
\textwidth=434pt
\oddsidemargin=16pt
\evensidemargin=0pt
\marginparwidth=42pt

% Mehr Parameter
\parskip=0.5\baselineskip \advance\parskip by \z@ plus 2pt
\parindent=\z@
\leftmargini=2em
\@ifundefined{mathindent}{}{\mathindent=\leftmargini}
\def\@listI{\leftmargin\leftmargini
   \topsep\z@ \parsep\parskip \itemsep\z@}
\let\@listi\@listI
\@listi
\def\@listii{\leftmargin\leftmarginii
   \labelwidth\leftmarginii\advance\labelwidth-\labelsep
   \topsep\z@ \parsep\parskip \itemsep\z@}
\def\@listiii{\leftmargin\leftmarginiii
    \labelwidth\leftmarginiii\advance\labelwidth-\labelsep
    \topsep\z@ \parsep\parskip \itemsep\z@}
\raggedbottom

% Index mit ^{...}
\def\specialh@t{\ifmmode\let\next=\sp\else\let\next=\index\fi\next}
{\catcode`\^=\active \global\let^=\specialh@t}
\def\@activehat{\catcode`\^=\active}
\let\dOcument\document \def\document{\dOcument\@activehat}

%
\newfont{\logo}{logo10 scaled\magstephalf}
\def\MF{{\logo META}\-{\logo FONT}}

%
\newfont{\quotefont}{cmssq8}
\newfont{\quoteslfont}{cmssqi8}
\newenvironment{chapterquote}{\bigskip\vbox{}\vfill
   \def\par{\ifhmode\/\endgraf\fi}\obeylines
   \def\TeX{T\kern-.2em\lower.5ex\hbox{E}\kern-.000em X}
   \def\author##1(##2){\smallskip\noindent\rm--- ##1\unskip\enspace(##2)}
%   \size{8}{9pt}\family{cmss}\series{q}\shape{n}\selectfont
   \quotefont\baselineskip9pt
   \parskip \z@ \parfillskip \z@ \interlinepenalty 10000
   \leftskip \z@ plus 40pc minus \parindent
%   \def\rm{\shape{n}\selectfont} \def\sl{\shape{it}\selectfont}
   \def\rm{\quotefont} \def\sl{\quoteslfont}
   \everypar{\sl}}%
 {\aftergroup\newpage}

\newenvironment{wide}{}{}

\input verbatim.sty
\def\noparskip{\par\vspace{-\parskip}}
\newenvironment{typed}{\noparskip\verbatim\advance\leftskip by\leftmargini}%
   {\endverbatim}
\let\wtyped\typed \let\endwtyped\endtyped
\newenvironment{example}{\begin{verse}}{\end{verse}}

\def\marginbox#1{\hbox to0pt{\hss\fbox{\bf#1}\,\,}}
\newenvironment{attention}{\par\leavevmode\marginbox{!!!}\small\ignorespaces}%
	{\par}
\newenvironment{aha}{\par\leavevmode\marginbox{Aha}\small\ignorespaces}{\par}

\def\K#1{\leavevmode\thinspace\hbox{\vrule\vtop{%
  \vbox{\hrule\kern1pt\hbox{\vphantom{\tt/}\thinspace{\tt
    #1}\thinspace}}\kern1pt\hrule}\vrule}\thinspace}
\def\<#1>{{$\langle$#1$\rangle$}}

\endinput
