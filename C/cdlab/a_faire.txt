
Ce a été fait depuis pas mal de temps :

- meilleure synchronisation des champs dans la fenêtre de copie (ok)
- détection de la vitesse de gravage (ok)
- correction du fonctionnement des boutons de cyclage (ok)
- Corriger le problème de non détection du graveur quand il n'est pas prêt (ok)
- bloquer le popup de sélection de vitesse si le programme n'est pas enregistré (ok)
- Implémenter les objets "Fichier audio" et le type DAO de fichiers audio (ok)
- Remettre la détection d'extraction audio TEAC par défaut par rapport
  à la détection MMC (ok)
- Ajouter une option "flush log file" dans l'onglet "debug" des préférences (ok)
- changement du format de la clé de cryptage (20 -> 16 caractères) (ok)
- Ajouter la possibilité de choisir le format audio (big/little/auto endian) (ok)
- Mettre en place une boite d'alerte pour annoncer les buffer underruns (ok)

Fait dans la version 0.30 :
- Corriger les 2 bombes quand le répertoire du fichier log n'existe pas
- Mettre systématiquement le nombre de retries en lecture
- Rajouter des boutons "éjection", "fermeture" et "redétection" dans les lecteurs
- Remettre et améliorer la détection automatique du type big/little endian
  (lecture type TEAC et PHILIPS uniquement)
- Gestion des Mode Select (10) et Mode Sense (10), si les MS(6) ne sont pas supportés
- Possibilité de variation du format audio (big/little) dans les images DAO
- Vérifier la copie en simul + gravage
- Vérifier la copie avec fichier intermédiaire
- Vérifier la copie avec même source et destination

Fait dans la version 0.31 :
- Correction d'un bug en copie de CD-ROM : on reformatait systématiquement au format TEAC.

Fait dans la version 0.32 :
- Gestion du mode SAO par défaut pour les graveurs supportant à la fois le SAO et le RAW.
- Correction du problème de démarrage de rotation pour l'effacement de CD-RW
- Correction du gravage TEAC.
- Correction de l'extraction audio TEAC.

Fait dans la version 0.40 :
- Mode RAW !!!!! Yark Yark Yark.
- Allocation de mémoire en ST Ram.
- Réinitialiser les lecteurs à la fermeture de device.
- Correction du bug d'igroration d'erreur en lecture sur le CDD3600.
- Correction d'un bug de mauvaise lecture de TOC sur lecteur Pioneer.
- Correction de la vérification du code de retour de la fonction get_device_list.
- Fonction de log du scan des périphériques.
- Rajout d'un type d'erreur "buffer underrun" pour certains graveurs Yamaha (8x).
- Mettre systématiquement le blanc en début de CD entre l'adresse 00:02:00
- Mettre à 0 le long mot de poids fort à l'ouverture d'un device

Fait dans la version 0.90 :
- Version Windom 09/2001
- Génération ISO de base.
- Mode TAO (graveurs MMC uniquement pour l'instant).

Reste à faire:
- Gestion des log_begin() et log_end() pour l'effacement des CD.
- Affichage de la durée des commandes dans les logs.
- Vérifier avant de graver si le CD est vierge (MMC only)
- Si on veut graver sur un CD-RW non vierge, demander confirmation avant d'effacer
- Fonction de vérification de la source (???)
- Copie multisession (extra CD et Dreamcast)
- Messages d'erreurs plus explicites (vraie boite de dialogue,
  affichage du sense code/sense key en clair)


Pas forcément à faire:
- Mettre en place la bufferisation des fichiers en lecture
- En plus de l'autodétection de lecteurs/graveurs, mettre en place une
  base de donnée par défaut pour les lecteurs/graveurs connus, avec possibilité
  d'exporter un lecteur/graveur inconnu.
- Mettre en place un "mode expert"
- Lors d'une copie de CD à CD, en mode non expert prévenir le danger
  et le début de la première piste, ou si possible, lire le début du CD
- Voir pour le splittage de fichiers images

- voir pour le problème de la première piste audio TEAC (???)
- en raw write, désactiver l'option "simulation" (à voir)
