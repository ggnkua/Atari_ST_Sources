énderungen Tools  Stand 180590
================

* ar, size, nm, globs, cc
  - long _STKSIZ = 8192;
    Stack vergrîûert, um mehr XARG-Argumente verarbeiten zu kînnen.

* cc
  - Directories in PATH, die mit '\' endeten (z.B. PATH=\bin\;bin\,
    fÅhrten zu Fehlern wie 'can't find Compiler hcc'. Fehler behoben.
  - Anzahl der Åbergebenen Dateien: öberlauf wurde nicht abgefangen.
    Fehler behoben.
  - Anzahl der max. bearbeiteten Dateien von 30 auf 60 erhîht
    (ld kann max. 60 Dateien).
  - Falls PATH leer ist (Desktop), wird default gesetzt.
  - Version von 1.01 auf 1.02

* nm
  - Library-Ende wurde nicht sicher erkannt, Fehlermeldung "bad header".
    Fehler behoben (Zeile 181).

