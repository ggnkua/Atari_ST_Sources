d l i b s  changes

memcpy.s :
len falsch addressiert

memcpy.s:
lmemcpy.s:
Adreûfehler, falls source oder destination auf ungeraden Adressen lagen.
06.05.90 memcpy.s neugeschrieben
06.05.90 lmemcpy.s AdreûprÅfung, schnelles byte copy hinzugefÅgt.

perror.c :
z 139 : fputs (msg); 2. Argument fehlte
02.05.90 fputs (msg,stderr);

dlibs.a:
06.05.90 Module entsprechend der Symbolreferenzen sortiert, so daû nur
ein Linker Pass nîtig ist.
Makefile erstellt.

system.c :
Fehler in parse_args. Argumentliste argv[] wurde nicht mit NULL abge-
schlossen, öberlauf wurde nicht abgefangen, XARG-Argumente waren auf
1024 Zeichen und 63 Argumente begrenzt.
18.05.90 Fehler behoben, XARG-Argumente sind nur durch freien Speicher
begrenzt.

putenv.c :
Environment wurde nicht mit Doppelnull abgeschlossen, falls eine zu
lîschende Variable die einzige im Environment war.
02.06.90 Zeile 59 hinzugefÅgt.

stat.c :
Fehler in access(): Schreibschutz wurde nicht richtig ausgewertet.
03.06.90 Fehler behoben.

pfindfil.c :
File wurde nicht gefunden, falls Environmentvariable PATH leer war
(ist in der Regel beim Desktop der Fall).
05.06.90 Fehler behoben (Zeile 18).

bzero.s :
Parameter buf, cnt wurden falsch adressiert. Fehler behoben.
10.06.90 Zeile 11 move.w statt move.l
         Zeile 12 a0 statt a1

