To compile the sprite program, use the following commands:-

cc -id: -od:yc.out sp3.c
as d:prg.s d:yc.out sprite.a -l d:gem.a d:lib.a -o sp3.prg
rm d:yc.out

Note that if you're using 'sixty' to change the sync rate you'll get flickery
sprites when they're near the screen top.
