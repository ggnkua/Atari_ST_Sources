Compilation of MGIF 4.0

Unfortunately there are no make files for any of the supported compilers.

Lattice C
  Precompile the file "proto.h"
  Load the project file "mgif_lc.prj" and chose MAKE in the menu.

Turbo C
  Select the project "mgif_tc.prj" and chose MAKE in the menu.

GCC
  Use any shell to invoke the commands:
     gcc -O2 -mshort -c *.c lineagcc.s
     gcc -mshort -o mgif.ttp *.o
  and perhaps:
     xstrip mgif.ttp
     toglclr mgif.ttp
