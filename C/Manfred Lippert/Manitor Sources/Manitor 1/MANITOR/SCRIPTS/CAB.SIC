if (address("manitor")) {

	include_prgs();
	show_prg("jinnee");
	show_prg("cab");

	aes(1, 1);	// AES, Returns
	gemdos(1, 1);
	xbios(0);	// Xbios aus
	bios(0);		// Bios aus

	vdi(1, 1);	// VDI, Returns

	activate();

}
