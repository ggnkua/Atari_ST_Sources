if (address("manitor")) {

	include_prgs();
	show_prg("jinnee");

	aes(1, 1);	// AES, Returns
	gemdos(0);	// Gemdos aus
	xbios(0);	// Xbios aus
	bios(0);		// Bios aus

	vdi(1, 1);	// VDI, Returns

	activate();

}
