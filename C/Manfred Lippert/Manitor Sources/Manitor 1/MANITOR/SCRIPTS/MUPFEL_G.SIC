if (address("manitor")) {

	aes(0);		// AES aus
	vdi(0);		// VDI aus
	xbios(0);	// Xbios aus
	bios(0);		// Bios aus

	include_prgs();
	show_prg("mupfel");
	show_prg("<???>");

	gemdos(1, 1);	// Gemdos, Returns

	activate();

}