if (address("manitor")) {

	deactivate();			// deaktivieren (falls er lief)
	preset();				// Grundeinstellungen
	vdi(0);
	include_prgs();
	show_prg("jinnee");
	show_prg("<???>");
	show_prg("noname");
	show_prg("calcfold");
	activate();				// Manitor aktivieren

}