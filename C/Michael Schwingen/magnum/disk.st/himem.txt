Kurzanleitung zu HIMEM.PRG

Falls die Magnum-ST-Karte mit einem 16MB-SIMM bestÅckt wird, werden 
auûer dem 'normalen' Fastram (welches von MAGNUMST.PRG angemeldet 
wird) zwei weitere Blîcke a 512KB eingeblendet. Diese Blîcke werden 
von HIMEM.PRG angemeldet und damit Programmen zugÑnglich gemacht.

Wenn man HIMEM.PRG an den Anfang des Auto-Ordners stellt und 
MAGNUMST.PRG an das Ende, so werden Auto-Ordner-Programme, bei denen 
dies mîglich ist, in die von HIMEM angemeldeten Blîcke geladen, so 
daû der von MAGNUMST angemeldete Speicher in einem mîglichst groûen, 
zusammenhÑngenden Block fÅr Anwenderprogramme zur VerfÅgung steht.

HIMEM kann mit MAGNCONF genau wie MAGNUMST konfiguriert werden.

