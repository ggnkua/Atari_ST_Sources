*** probleme dans fcm_init_aes_vdi()
    quelle valeur a mettre dans workin[0] pour l'ouverture de la station vdi ???

---------------------------------------------------------------




[Re: [Emutos-devel] VDI always in ST High in TOS 4.04]   17/10/2018

    Sidenote: TOS.HYP says -- as many other documentation -- to set the
    device driver number in v_opnvwk() to 2 + Getrez(). However, I wonder if
    this is still correct for the Falcon with it's many screen modes.

And your wonder has definitely paid off! I remembered about Johan Klockars' recommendation to use '1' as 'current resolution' in work_in[0] and guess what? It works!

So as a side note I'd way that all apps calling work_in[0] = Getrez() + 2 on Falcon are doing it terribly, terribly wrong.

-- 
MiKRO / Mystic Bytes


