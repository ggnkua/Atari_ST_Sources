main()
{
    a = "ABCDabcd";
    len = sizeof(a);
    for (i = 0; i < len; ++i)
	print(a[i],"\n");
    for (i = 0; i < len; ++i)
	if (a[i] >= 'a' && a[i] <= 'z')
	    a[i] = a[i] - 'a' + 'A';
    print(a,"\n");
}
