
to spiral(len,turn,max)
{
int i

for i = len to max
	{
	right(turn)
	forward(i)
	}
}

int color

tographics()
for color = 4 to 10
	{
	pencolor(color)
	spiral(1,color+20,35)
	}
waitkey()
