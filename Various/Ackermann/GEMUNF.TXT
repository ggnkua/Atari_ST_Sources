   Here is an unformatter( removes '\n' except at end of paragraph)
that does not combine words at the deleted '\n'.  No more thisis
because this was at the end of a line.

	Usuage	enter filenames at the gem file selectors, and wait for
the results.

	How is that for simple docs.

	Source to be published as a 'simple gem' program.  Source is ANSI
C in a flex file.
