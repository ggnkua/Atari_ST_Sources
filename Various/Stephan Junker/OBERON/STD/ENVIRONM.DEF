MODULE Environment; (* SJ *)

(* works with the environment variables *)


PROCEDURE ReadStr*(Name : ARRAY OF CHAR; VAR String : ARRAY OF CHAR) : BOOLEAN;

(* reads the string following the environment variable which is the
   contents of the variable.
*)

PROCEDURE ReadInt*(Name : ARRAY OF CHAR; Base : INTEGER;
                   VAR Value : LONGINT) : BOOLEAN;

(* reads the integer value of an environment variable. Base is the number
   base (10 = decimal, 16 = hex etc.).
*)


END Environment.