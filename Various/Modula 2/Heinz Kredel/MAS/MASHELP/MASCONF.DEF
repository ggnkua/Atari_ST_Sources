
(* MAS Configuration definition module. *)
DEFINITION MODULE MASCONF;
(* Import lists and Definitions *)
  FROM MASELEM IMPORT GAMMAINT;
  FROM Portab IMPORT STRING;
  VAR
    KBCell : GAMMAINT;
                      (* Requested cell storage in kilo byte. *)
   (* InitName : STRING[63]; *)
    InitName : STRING;    (* Topspeed-Change *)
                      (* Name of initialization data set. *)
    CellMem : STRING; (* Name of temporary data set of cell space. *)

  END MASCONF.
