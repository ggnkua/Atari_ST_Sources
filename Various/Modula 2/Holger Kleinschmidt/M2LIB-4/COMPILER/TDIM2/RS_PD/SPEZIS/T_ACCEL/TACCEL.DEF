DEFINITION MODULE TAccel;                          (* Terminal-Accelerator.*)
(* Ω 1992-96 ROLF SCHRADER.                            * 21.9.92, 28.10.92 *)

(* Ersetzt im Modul Terminal die GEMDOS-Con-IO durch andere:  Dadurch wird *)
(* Terminal (& damit auch InOut & Co.) erheblich schneller.  Mit BIOS oder *)
(* VDIESC fehlen dann allerdings die (GEMDOS-) I/O-Umlenkung (wichtig fÅr  *)
(* manche Shells) sowie die Control-Mîglichkeiten (^C,^S,^Q,...); man kann *)
(* den Modul also auch einsetzen, wenn diese Dinge unerwÅnscht sind ...    *)

CONST     (* Dips: *) STATE = 0; READ = 1; WRITE = 2; WSTR = 3;
          (* IoTyp:*) GEMDOS = 0; VIRTUAL = 1; BIOS = 2; VDIESC = 3;

PROCEDURE Switch (IoTyp: INTEGER; Dips: BITSET);
              (* Default (Init. im Modul-Body):  AbhÑngig vom NVDI-Cookie; *)
              (*   ohne N. oder V<2.10: BIOS {0..3}, sonst VIRTUAL {WRITE}.*)

(* Bem.:  Beim Einsatz nach Import von InOut muû man dessen Init erneuern! *)

END  TAccel .
