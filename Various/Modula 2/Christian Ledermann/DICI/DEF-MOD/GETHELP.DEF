DEFINITION MODULE GetHelp;

FROM PORTAB     IMPORT SIGNEDWORD;


PROCEDURE CallSTGuide(ID: SIGNEDWORD; Text, Topic : ARRAY OF CHAR):BOOLEAN;
(* Ruft den ST-Guide auf. 					*)
(* Text ist der zu ladende Hypertext und Topic der Seitenname 	*)
(* RÅckgabewert ist TRUE falls ST-Guide installiert ist, FALSE 	*)
(* wenn ST-Guide nicht gefunden wird.				*)

END GetHelp.
