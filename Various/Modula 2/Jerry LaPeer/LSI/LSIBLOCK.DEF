DEFINITION MODULE LSIBlock;

FROM SYSTEM IMPORT BYTE,ADDRESS;

PROCEDURE FillMemW(Block : ADDRESS;
                   Count : LONGCARD;
                   Value : BYTE);

PROCEDURE MoveMemW(ToBlock   : ADDRESS;
                   FromBlock : ADDRESS;
                   Count     : LONGCARD);
                   
END LSIBlock.
