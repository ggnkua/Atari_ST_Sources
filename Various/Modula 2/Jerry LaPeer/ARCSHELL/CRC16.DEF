DEFINITION MODULE CRC;

EXPORT QUALIFIED Result, Compute;

VAR Result: CARDINAL;

PROCEDURE Compute(Ch: CHAR);
(*
 * Compute the next cycle of the CRC-16 calculation.  The result is left in
 * "Result".  Assign "Result" to zero before the first cycle.
 *)

END CRC.
