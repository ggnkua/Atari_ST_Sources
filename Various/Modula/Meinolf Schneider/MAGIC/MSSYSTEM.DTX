DEFINITION MODULE MSSystems;

(*----------------------------------------------------------------------------
 * System-Version: MOS 3.5
 *----------------------------------------------------------------------------
 * Version       : 1.0
 *----------------------------------------------------------------------------
 * Text-Version  : V#00001
 *----------------------------------------------------------------------------
 * Modul-Holder  : Meinolf Schneider
 *----------------------------------------------------------------------------
 * Copyright May 1990 by Digital Art Meinolf Schneider
 *----------------------------------------------------------------------------
 * MS  : Meinolf Schneider
 *----------------------------------------------------------------------------
 * Datum    Autor Version Bemerkung (Arbeitsbericht)
 *----------------------------------------------------------------------------
 * 01.05.90 MS    1.0     Grundversion
 *----------------------------------------------------------------------------
 * Modul-Beschreibung:
 *
 * Sammlung wichtiger Grundfunktionen fÅr die TOS-Magazin-Gimmicks.
 *----------------------------------------------------------------------------
 *) (*$C-,M-,R-,S-,N+*)

FROM    System                  IMPORT  ADDRESS, ADR, BYTE;


(*-------------------- schnelle Superfunktion --------------------------------*)
        
PROCEDURE EnterSupervisorMode;
PROCEDURE RestallSuperTrap;


(*-------------------- eigene kleine Speicherverwaltung ----------------------*)

PROCEDURE Allocate ( VAR addr    : ADDRESS;
                         siz     : LONGCARD );

PROCEDURE MemAvail(): LONGCARD;
(*
 * Liefert die maximal noch verfÅgbare, zusammenhÑngende Anzahl Bytes.
 *)
 
(*--------------------- BCONOUT - Textausgabe --------------------------------*)

PROCEDURE Write ( c     : CHAR );
PROCEDURE Writeln;
PROCEDURE WriteString ( Text    : ARRAY OF CHAR );


(*--------------------- schnelle Zufalls-Funktion ----------------------------*)

PROCEDURE MinMaxRandom ( Min, Max       : INTEGER ): INTEGER;
(*
 * Erzeugt eine Zufallszahl, die sich in den angegeben Grenzen von
 * [Min] und [Max] befindet. Der Wertebreich dieser Funktion schlieût
 * die vorgegebenen Werte mit ein.
 * Es sind auch negative Werte mîglich.
 * Ist der [Min]-Wert grîûer als der [Max]-Wert, so ist der Wertebereich
 * undefiniert. Ist [Min] und [Max] gleich, so ist der resultierende Wert
 * immer gleich [Min] und [Max].
 *
 * Es muû gelten: ([Max] - [Min]) <= 32768
 *
 * Beispiel:
 *
 * MinMaxRandom ( 4, 8 )  ===> z.B   8, aber niemals z.B. 9
 * MinMaxRandom ( 4, 8 )  ===> z.B   6
 * MinMaxRandom ( 7, 7 )  ===>       7
 *)


(*----------------------------------------------------------------------------*)


END MSSystems.


