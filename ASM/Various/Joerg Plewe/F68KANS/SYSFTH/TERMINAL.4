VARIABLE ROWS           ( AVAILABLE COLUMNS ON TERMINAL	 )
VARIABLE COLUMNS        ( AVAILABLE ROWS ON TERMINAL     )

DECIMAL
25 ROWS !     80 COLUMNS !

: BELL ( -- )               7 EMIT ;

: BACKSPACE ( -- )          8 EMIT  -2 out +! ;
: BACKSPACES ( N -- )       0 ?DO  BACKSPACE  LOOP ;

: TAB ( N -- )
        out @ - DUP 0< IF  NEGATE BACKSPACES  EXIT THEN
        SPACES ;

: ?CR ( N -- )    out @ < IF  CR  THEN ;
: ?16CR ( -- )    COLUMNS @ 16 - ?CR ;  ( 16 CHARS FREE TO EOL?	 )


