                   Anhang zum OverScan-Handbuch
                 ================================

Handbuch Abschnitt 1.5 (Systemvoraussetzungen)
----------------------------------------------
   Folgende TOS-Versionen werden zusÑtzlich zu den im Handbuch 
   angegebenen Versionen unterstÅtzt:
   
     TOS 2.06 und  2.05, das sog. TOS 1.7, das auf die Hardware 
     des STs angepaûte TOS 1.6, das PAK 68020 TOS 1.4 von der CT,
     das KAOS-TOS 1.42 von Markt&Technik und das BTOS 1.4 der
     Hypercache-Karte.
     
Handbuch Abschnitt 2.5 (Tips und Tricks zur Hardware)
-----------------------------------------------------
   Wenn es beim MEGA-ST nach lÑngerem  Betrieb zu leichten Flimmer-
   stîrungen auf  dem Bildschirm  kommen sollte,  dann durchtrennen
   Sie ein Beinchen des  Keramik Kondensators,  der der Stiftleiste
   am nÑchsten liegt. 

   Unsere Schaltung puffert das  VSync-Signal an der Monitorbuchse.
   Daher kînnen Sie nicht mehr von  auûen  (fÅr Genlockanwendungen)
   VSync in die Monitorbuchse einspeisen! 
   Wenn Sie trotzdem  Genlock zusammen mit  AutoSwitch-OverScan be-
   treiben mîchten,  dann kînnen Sie  dies durch  einen  einpoligen
   Umschalter  erreichen,  der  in  die  VSP-Leitung  eingeschleift
   wird:

    +---------------------------------------+                                    
    !                                       ! Genlock
    !      +-------------------+            o             +----------+
    !      !                   !                          ! Monitor- !
----*----->! OverScan-Hardware !------>-----o===o---------! Buchse   !
     VSync !                   !  VSP    Normal    VSP    ! VSync    !
           +-------------------+                          +----------+

                    Bild 2.5.1: Schalterstellung "Normal"


    +---------------------------------------+
    !                                       !                                    
    !                                       o\ Genlock
    !      +-------------------+             \\           +----------+
    !      !                   !              \\          ! Monitor- !
----*----->! OverScan-Hardware !------>-----o  \o---------! Buchse   !
     VSync !                   !  VSP    Normal    VSP    ! VSync    !
           +-------------------+                          +----------+

                    Bild 2.5.2: Schalterstellung "Genlock"

   Der Schalter  darf  natÅrlich  nur  umgelegt  werden,  wenn  der 
   Rechner abgeschaltet ist.  Denkbar wÑre  aber auch ein elektron-
   ischer Umschalter,  der  z.B.  Åber ein  externes oder  internes 
   Signal  umgeschaltet  wird.  Bei  entsprechender  Programmierung 
   kînnte dann auch wÑhrend des Betriebes umgeschaltet werden.  Bei
   einigen Rechner hat Atari das HSync-Signal gepuffert  (meist mit
   einem 74xx04) und verhindert damit Genlock.  Bei diesen Rechnern
   ist ebenfalls  eine  Schaltung nach obigem  Muster erforderlich,
   die  nach  der  Pufferung  in  die  HSync-Leitung  eingeschleift 
   werden muû.

Handbuch Abschnitt 3-2-2 (Positionieren des Bildschirms)
--------------------------------------------------------
   Sie kînnen die Box mit den Cursortasten in  Hîhe und Breite ver-
   Ñndern.  Die linke obere Ecke  kînnen  Sie mit Shift-Cursortaste
   frei auf dem Bildschirm verschieben. 

Handbuch Abschnitt 3-3-3 (Tasten mit Sonderfunktion)
----------------------------------------------------
   Die Taste 'B' entfÑllt,  da die Hardcopy-Treiber nun nicht  mehr
   im OVERSCAN.PRG  integriert sind,  sondern als externe Programme
   gestartet werden.

   Mit der Taste 'X' kann im Farbbetrieb auf externe Syncronisation
   umgeschaltet werden. Dies ist zB beim Genlock-Betrieb notwendig.

Handbuch Abschnitt 3-4 (Das Format der OverScan.Inf Datei)
----------------------------------------------------------
   Das neue SchlÅsselwort ROMPORT:  bestimmt den Modus von Program-
   men, die sich in Rom-Moduln befinden.
      
Handbuch Abschnitt 3-4-2 (Weitere Einstellungsmîglichkeiten)
------------------------------------------------------------
   BIT_IMAGE: 
     Das SchlÅsselwort entfÑllt,  da die Hardcopy-Routinen  nun als
     extra Programme vorliegen. 
     
   TOS14FIX:
     Mit dem SchlÅsselwort TOS14FIX:  bestimmen Sie, ob bei TOS 1.4
     der integrierte TOS14FIX-Patch benutzt werden soll.
       Default: Aus	  
       
   VDIFIX:
     Mit dem SchlÅsselwort kînnen Sie den integrierten VdiFix-Patch
     aktivieren.
       Default: Aus

   GEM_ENV:   
     Mit diesem SchlÅsselwort kînnen Sie bestimmen,  ob das GEM mit
     einem neuen Environment gestartet  werden soll. Dazu  wird die
     Datei OVERSCAN.ENV nachgeladen,  die sich auch im  Auto-Ordner
     befinden muû. Im Environment kînnen Sie z.B. einen neuen Such-
     pfad vorgeben, in dem GEM Resource-Files sucht.  
        Default: Aus
        
   SHELL0:    
     Mit dem SchlÅsselwort  kînnen Sie das  integrierte  Shell0.Prg
     aktivieren, welches bei der Benutzung von Shell's (zB. GEMINI)
     benîtigt wird.
        Default: Aus
        
   IKBDSYS:
     Mit diesem SchlÅsselwort  kînnen Sie  bestimmen,  ob sich  der  
     OverScan-Treiber in den IKBDSYS-Vektor  einhÑngt.  Bei manchen
     Programmen (zB. EMACS.TTP),  die  sich auch in  diesen  Vektor 
     hÑngen, kann es zu Zeitproblemen kommen, da der IKBD-Interrupt
     sehr zeitkritisch ist.  Treten also  Probleme im  Zusammenhang 
     mit TastendrÅcken oder Mausbewegungen  auf,  so kînnen Sie den
     Treiber  aus dem Vektor  durch  'IKBDSYS: -' fernhalten.  Dann
     funktioniert allerdings die  Bildschirmrand-Lîsch-Tastenkombi-
     nation Shift-Shift-ClearHome nicht mehr. 
        Default: An
     
   QUICKST2:
     Durch dieses  SchlÅsselwort  wird  die  QuickST 2.1  Anpassung 
     aktiviert.  Die SchlÅsselworte  'DESKTOP:'  und 'AUTO:' werden 
     nicht mehr beachtet, es wird fÅr beide der OverScan-Modus vor-
     gegeben.
        Default: Aus
        
   CONS_FIX:
     Aktiviert den  integrierten Consolen-Fix Patch.  Dieser  sorgt
     unter QuickST 2.1 dafÅr,  daû die Ausgabe-Umlenkung  in Shells
     weiterhin funktioniert.
        Default: Aus

   QUICK_GEM_ONLY:
     Mîchte man das Blitzen von QuickST 2.1 unterbinden,  so mÅssen
     Sie auf die  Beschleunigung von  TOS-Ausgaben verzichten.  Und
     genau dazu dient dieses SchlÅsselwort.   
        Default: Aus
     
   NO_INTRO:
     Mit dem SchlÅsselwort  NO_INTRO:  kînnen Sie das Intro und den
     Sound  beim  Installieren  des  OverScan-Treibers ausschalten.
	    Default: Aus

   EXTERNAL_SYNC:
     Mit diesem SchlÅsselwort kann die Umschaltung auf ein externes
     Sync-Signal eingeschaltet werden. Beim Betrieb mit dem Genlock
     muû das SchlÅsselwort gesetzt werden.
        	    
   Zu TOS14FIX:, VDIFIX:, CONS_FIX:  und SHELL0: finden Sie nÑheres
   im  Abschnitt  'Integrierte Patchprogramme',  die  von  PATCHES: 
   betroffenen Funktionen  werden  im  Abschnitt 'Programm-Patches' 
   aufgefÅhrt. 
   Die SchlÅsselworte 'QUICKST2:' und  'QUICK_GEM_ONLY:' werden  im
   Abschnitt 'OverScan und QuickST' nÑher erlÑutert. 
   Die Bedeutung von  EXTRA_MEMORY:  wird im Abschnitt  'Umschalten
   der Auflîsung' nÑher erlÑutert.

   Der NDC-Modus (NDC: Normal-Device-Coordinates) des  GDOS ist ein
   spezieller Modus der  Koordinatenangabe.  Die Ausgaben  erfolgen
   nicht in Bildschirmkoordinaten (RC: Raster-Coordinates), sondern
   in einem virtuellen  Koordinatensystem  das in X- und Y-Richtung
   von -32767 ... +32768 reicht.

   Die  beim Farbmonitor mit 60_HERTZ:  vorgegebene  Bildschirmfre-
   quenz  wird   hinter  jedem  Programm  neu  initialisiert.   Ein 
   CHANGEHZ.PRG ist daher nicht mehr notwendig.

   Normalerweise besitzt der  ATARI im Farbmodus einen weiûen Rand. 
   Dieser ist  unter  OverScan aber nur  sehr  klein  gegenÅber dem
   wirklichen schwarzen Darstellungsrand.
   Sie  kînnen  ihn  daher  optisch  beseitigen,  in  dem  Sie  mit 
   BLACK_BORDER: einen schwarzen Rand einstellen. Beim ZurÅckschal-
   ten in den Normalmodus wird wieder ein weiûer Rand benutzt.

Neues Abschnitt Erweiterte Features im Treiber 
----------------------------------------------
   Wenn  das  SchlÅûelwort  IKBDSYS:  eingeschaltet  ist,  kann der
   Bildschirm  mit  der  Tastenkombination  Shift-Sift-Rechts/Links
   verschoben werden.  Dies ist insbesondere fÅr Besitzer von Moni-
   toren  interessant,  bei  denen  das Bild im kalten Zustand eine 
   andere horizontale Position einnimmt,  als im warmen Zustand des
   Monitors. Das Bild sollte nicht wÑhrend Bildschirm-Ausgaben ver-
   schoben  werden,  da  dies  zu Stîrungen des Bildaufbaues fÅhren
   kann.
   
   Auch  das  OverUtil.Acc  unterstÅtzt das horizontale Verschieben
   des Desktops.

Neuer Abschnitt OverScan und QuickST
------------------------------------
   Der OverScan-Treiber  enthÑlt  eine  spezielle  Anpassung an den 
   Beschleuniger QuickST 2.10.  Dieser funktioniert unter OverScan,
   aber leider wird ein  ZurÅckschalten  auf den  Normalmodus nicht
   erkannt.
   Aus  diesem  Grunde wird der QuickST-Treiber ausgeschaltet, wenn
   auf den Normalmodus umgeschaltet wird.  Die  Beschleunigung  der
   Ausgaben ist also nur im OverScan-Modus wirksam (GEM-Performance
   ohne Blitter 250%, mit Blitter 295% !).
   Die Umschaltroutine wird  durch  das SchlÅsselwort 'QUICKST2: +'
   aktiviert. Sie kînnen nun aber den Modus fÅr den Auto-Ordner und
   fÅr den Desktop nicht mehr  frei festlegen.  Es wird im  Desktop
   und im Auto-Ordner der OverScan-Modus benutzt,  egal was Sie mit 
   'DESKTOP:' oder 'AUTO:' vorgegeben haben.

   Unter QuickST 2.1 gibt es Probleme mit der  Ausgabe-Umlenkung in
   einigen Shells (zB. GEMINI). QuickST gibt Ausgaben immer auf den
   Bildschirm aus, auch wenn die Shell sie zB. in ein  Fenster oder
   eine Datei umlenkt. Der Cons_Fix-Patch  ÅberprÅft, ob solch eine
   Umlenkung stattfindet  und  gibt  in diesem  Falle die  Ausgaben 
   nicht an QuickST weiter, sondern springt weiter in das TOS.
   Manche  Programme (mit TurboC kompiliert wurden)  haben Probleme
   unter QuickSt, wenn sie  Ausgaben machen.  Dieses liegt an einem
   Fehler  in der TurboC  Bibliothek, die  das A2-Register bei Aus-
   gaben nicht  sichert. Auch dieser Fehler wird durch Cons-Fix be-
   hoben.
   Der Patch wird durch das SchlÅsselwort  'CONS_FIX: +' aktiviert.

   QuickST 2.1 ist  noch nicht  vollstÑndig  an  OverScan angepaût. 
   Beim Scrollen oder  auch  beim Lîschen von  Zeilen  im TOS-Modus
   blitzt der Bildschirmrand kurz auf.  Dies kommt  daher, daû  der
   Rand durch QuickST auf weiû  gesetzt wird.  Diese  weiûe  FlÑche
   fÑllt aber unter OverScan in den ZeilenrÅcklauf des Monitor's. 
   Der OverScan Treiber ÅberprÅft stÑndig den Rand,  ob er noch auf 
   schwarz gesetzt und somit  keine RÅcklaufstrahlen sichtbar sind.
   Wenn nicht, wird der Randbereich wirder auf schwarz gelîscht. 	  
   Wenn Sie diesen Fehler unterdrÅcken mîchten, mÅssen QuickST alle
   TOS-Ausgaben entzogen werden. Dies kînnen Sie mit dem SchlÅssel-
   wort 'QUICK_GEM_ONLY: +' erreichen. Das Setzen von 'CONS_FIX: +'
   ist logischerweise ÅberflÅssig, da QuickST ja keine TOS-Ausgaben 
   mehr erreichen.

Neuer Abschnitt OverScan und NVDI
---------------------------------
   NVDI ist das deutsche Nachfolgeprodukt zu TurboST, welches nicht
   unter OverScan lief. Das NVDI bedarf keinerlei Anpassung seitens
   OverScan,  es werden sogar  beide  Modi  des AutoSwitch-OverScan
   beschleunigt. (ACHTUNG:  NVDI 1.0 funktioniert nur korrekt, wenn 
   die Pixel pro Zeile  durch 32 teilbar sind,  also: 672, 704, 736
   und 768 Pixel!)

   Das SchlÅsselwort NDC: ist unnîtig, da der NDC-Modus mit NVDI in
   beiden Auflîsungen  korrekt funktioniert. Die  Patches  VDIFIX:,
   QUICK_GEM_ONLY:  und CONS_FIX:  sind  auch  ÅberflÅssig, da  die 
   durch  diese  Patches behobenen Fehler mit NVDI nicht auftreten.
   
   Das  NVDI.PRG muû vor OverScan im Auto-Ordner ausgefÅhrt werden.
      
Neuer Abschnitt Antworten auf beliebte Fragen
---------------------------------------------

   - Warum  kann man den Bildschirm im Setup nicht pixelweise ver-
     schieben ?
   Den Bildschirm kînnen Sie horizontal nur in 16er Schritten ver-
   schieben,  weil  von der Hardware des STs her immer 16 Pixel in
   einem Speicherwort zusammengefasst sind.
    
   - Warum kann man nicht mitten im Programm die Auflîsung wech-
     seln ?
   Die  meisten  Programme  initialisieren die Breite und Hîhe nur
   wÑhrend des Startvorgangs.  WÅrde man diese Wertze zur Laufzeit 
   verÑndern,  so  wÅrden  die Programme die Fenster und Dialog an
   falschen Positionen îffnen. Die Bildschirmausgaben wÑren falsch 
   positioniert,  insbesondere wÅrden alle Ausgaben, die direkt in
   den Bildschirmspeicher  geschrieben  werden,  Stîrungen auf dem 
   Bildschirm verursachen.
   
   - Woran  liegt  es, wenn es beim Starten mancher Programme oben 
     rechts in der MenÅleiste aufblitzt ?
   Bei diesen Programmen ist die Breite der MenÅleiste falsch vor-
   gegeben.  Durch  das  SchlÅsselwort  'PATCHES: +' wird die ent-
   stehende	Stîrung sofort wieder gelîscht.
   
   - Liegt  es  an  OverScan, wenn ein Programm nicht zusammen mit
     OverScan funktioniert ?
   Funktioniert  ein Programm nur im OverScan-Modus nicht korrekt,
   so  liegt es am jeweiligen Programm.  Bitte informieren Sie den
   Programmierer  bzw. den Vertrieb und fragen Sie, ob schon ange-
   paûte Update vorhanden sind.
   
   Nur  wenn  ein  Programm in beiden Modi des AutoSwitch-OverScan
   nicht funktioniert (aber ohne den OverScan-Treiber), kann es an
   OverScan  liegen.  Bitte nehmen Sie in solch einem Fall Kontakt
   mit  uns auf.  Stellen Sie vorher sicher,  daû der Fehler nicht 
   Aufgrund anderer installierter Programme auftritt.  Dies kînnen
   Sie tun, indem Sie den Rechner in einer minimalen Konfiguration
   nur  mit  dem  OverScan-Treiber und ohne Accessories booten und 
   dann das Programm noch einmal testen.
   
Handbuch Abschnitt 3-7 (Die Patchvariablen von OverScan)
--------------------------------------------------------
   Die Angaben des zusÑtzlich zum normalen  Bildschirmspeicher not-
   wendigen Speicher's bei den Patchvariablen ist nicht korrekt.

     Offset | LÑnge | Default |   
     -------+-------+---------+-----------------
       $1A  | Long  | $3D00   | Monochrom
       $1E  | Long  | $7A00   | Colormodus

   Es wird also 2 bzw. 3.5 K Bytes weniger Speicher benîtigt als im
   Handbuch angegeben.

   Bei den Werten, die Tastenkombination betreffen, setzen sich die
   Zahlen folgendermaûen zusammen:
   
       Wert | Taste
      ------+-------
         1  | Rechts-Shift
         2  | Links -Shift
         4  | Control
         8  | Alternate
          
   Der  eingetragene  Wert  besteht  aus  der Summe der notwendigen
   (bzw. mîglichen) Sondertasten.  Der Wert 6 bedeutet  also Links-
   Shift/Control.   
   
Handbuch Abschnitt 3-13 (Liste der mitgelieferten Dateien)
----------------------------------------------------------
   Es werden nun einige zusÑtzliche Programme mitgeliefert. Die Be-
   schreibung der neuen Programme erfolgt weiter am Ende dieser Da-
   tei.
   
    --------------------------+-------------------------------------
     /AUTO/OVERSCAN.ENV       | Beispiel GEM-Environment
     /HARDCOPY/OVER_IMG.PRG   | Hardcopy-Treiber fÅr Bit-Images
     /HARDCOPY/OVER_EPS.PRG   | Hardcopy-Treiber fÅr Epson-Drucker
     /HARDCOPY/OVER_24N.PRG   | Hardcopy-Treiber fÅr 24-Nadel-Drucker
     /HARDCOPY/OVER_DJT.PRG   | Hardcopy-Treiber fÅr HP Diskjet+
     /HARDCOPY/OVER_LAS.ACC   | Patch fÅr DMC-LaserBrain
     /OV_PATCH/OVER_FSB.PRG   | Patch fÅr FileSelectorBoxen	 
     /OV_PATCH/OVER_WIN.PRG   | Patch fÅr Fenstergrîûen-Fehler
     /OV_PATCH/OVER_EXC.PRG   | Patch fÅr die Bombenroutine
     /SOFTWARE/OVER_LOW.S     | Assembler-Source fÅr LowLevel-Support
     /OVERUTIL.ACC            | Bildschirmschoner & Desktopuhr
     /OVERDEMO.PRG            | Demo zum automatischen Umschalten

Neuer Abschnitt Integrierte Patchprogramme
------------------------------------------
   Da der Autor von  OverScan  einige Programme  zur  Behebung  von
   Betriebsssystemfehlern  verîffentlicht  hat, lag  es nahe, diese 
   direkt im OverScan.Prg zu integrieren.

   Tos14Fix
   --------
     Die vom Tos14Fix.Prg vorgenommenen Patches werden  bei TOS 1.4 
     auch  vom OverScan.Prg  durchgefÅhrt.  Dabei wird der Orginal-
     Patchbereich bei $600 benutzt, um den LineF-Trap umzuhÑngen.
     
     Mit  dem  SchlÅsselwort  'TOS14FIX: -' kînnen Sie diesen Patch 
     ausschalten,  wenn Sie den  Fehler schon im ROM behoben haben. 
   VdiFix
   ------
     Der Fehler beim ôffnen von Vdi-Workstations in TOS 1.0 und TOS
     1.4 wird vom OverScan.Prg behoben.  Bei BlitterTOS 1.2 ist der
     Patch inaktiv.  Das VdiFix Programm ist  daher nicht mehr not-
     wendig.   
     
     Mit  dem  SchlÅsselwort  'VDIFIX: -'  kînnen  Sie diesen Patch 
     ausschalten,  wenn Sie den  Fehler schon im ROM behoben haben. 
   Cons_Fix
   --------
     Mit den  Beschleunigern  QuickST und TurboST funktioniert  die
     Ausgabe-Umleitung unter GEMINI oder anderen Shells  nicht mehr
     korrekt.   Das  Cons_Fix.Prg  verhindert,  das  Ausgaben,  die
     eigentlich  in  ein  Shell-Fenster  umgeleitet  wurden,  durch
     QuickST oder TurboST abgefangen und beschleunigt werden. 
     Mit dem SchlÅsselwort 'CONS:FIX: +' wird der in OverScan inte-
     grierte Consolen-Fix-Patch benutzt.
   Mshrink
   -------
     Bei einem 'Mshrink(add,0L)'  kînnen bei  TOS 1.4 Fehler in der
     internen Speicherverwaltung auftreten.  OverScan wandelt  alle
     'Msrink(add,0L)' in 'Mfree(add)' um.
   Shell0
   ------
     Durch  das  Shell0.Prg  wird der  Shell-Vektor beim Booten und 
     beim Reset zurÅckgesetzt.  Dieses ist fÅr Benutzer der GEMINI-
     oder Master-Shell notwendig.
     Mit dem SchlÅsselwort 'SHELL0: +'  wird das Lîschen des Shell-
     Vektors durch das OverScan.Prg ausgefÅhrt.
   
Neuer Abschnitt Programm-Patches
-------------------------------- 
   Die  folgenden  Patches  kînnen   Sie  Åber   das  SchlÅsselwort 
   'PATCHES: -'  oder im  InstallationsmenÅ abgeschalten,  wenn Sie
   Ihre eigenen Programme auf Korrektheit testen wollen.  Es laufen
   dann aber  weniger Programme,  da abgefangenen Fehler doch recht
   hÑufig in den verschiedensten Programmen gemacht werden.

   vro_cpyfm
   ---------
     Wird  in  der  MFDB-Struktur  als Adresse der Bildschirmanfang
     eingetragen, so wird dieser Wert gegen einen Null-Pointer aus-
     gewechselt, da die meisten Programm die Breite in Words falsch
     angeben. (Als Pixel/16, was unter OverScan nicht stimmt)
   Physbase
   --------
     Manche Programme benutzen die Funktion 'Physbase',  um die An-
     fangsaddresse des Bildschirms zu erfahren, was falsch ist.

     Physbase  liefert  daher  den  Logbase-Wert  des dargestellten 
     Bildschirms und  nicht den realen Wert,  der  mit einem Offset 
     behaftet ist.
   Setscreen
   ---------
   Ein éndern der  Bildschirmadresse und  ein Wechsel der Auflîsung
   ist nicht gestattet,  nur das Verlegen des logischen Bildschirms
   mit 'Setscreen(xxxx,-1L,-1)' ist zulÑssig. 
   
   Die Programme  wÅrden  beim  Verlegen der Videoadresse oder beim 
   Wechsel der Auflîsung die notwendigen Offsets und  auch die neue
   Grîûe des Bildschirmspeichers nicht beachten.

Handbuch Abschnitt A-1 (AES)
----------------------------
   Farbebenen
   ----------
      Die Anzahl der Farbebenen bekommen Sie in 'global[10]' mitge-
      teilt.
      
Handbuch Abschnitt A-2 (VDI)
----------------------------
   Farben
   ------
     Die Anzahl der  gleichzeitig  verfÅgbaren Farben  bekommen Sie 
     beim 'v_opnvwk'-Aufruf in WorkOut[13] mitgeteilt.

     Sie dÅrfen die Farben nicht mit den XBios-Funktionen  Setcolor
     oder Setpallette setzen, sondern mit der Funktion ''vs_color',
     die von allen Grafikkarten unterstÅtzt wird. 
     
     Zur Abfrage der  eingestellten Farben kînnen  Sie die Funktion
     'vq_color' verwenden.    

Handbuch Abschnitt A-5 (Spezielle Probleme)
-------------------------------------------
   Desktop-Uhren
   -------------
     Bevor  eine Desktop-Uhr  in den Bildschirm-Speicher  schreibt, 
     sei es nun mit 'v_gtext' oder mit einer Assembler-Routine, muû
     sie  ÅberprÅfen,  ob  nicht  der logische  Bildschirm  verlegt
     wurde. Manche Programme Ñndern  die Bildschirmadresse,  um mit 
     VDI-Funktionen  ein  Bild (Dialogboxen usw...)  unsichtbar  zu
     zeichnen.  Sobald die Zeichenaktion fertig ist,  wird das Bild
     mit  'vro_cpyfm'  in  den   dargestellten   Bildschirmspeicher
     kopiert. Vor den Ausgaben sollte  man  deshalb  'Logbase'  und
     'Physbase' vergleichen. Da bei Groûbildschirmen die 'Logbase'-
     und 'Physbase'-Werte  unterschiedlich sind,  muû der Vergleich
     Åber die  Differenz der  beiden  Funktionen stattfinden.  Beim 
     Programmstart vermerkt man  diese Differenz und  kann dann vor
     jedem Zeichenvorgang  mit  'Logbase' - 'Physbase' feststellen,
     ob ein  Zeichnen mîglich ist.  Das Verlegen von 'Logbase' wird
     z.B. von That'sWrite und Calamus benutzt.
   Bildschirmschoner
   -----------------
    FÅr   Bildschirmschoner  gilt  dasselbe,  was   im   vorherigen
    Abschnitt erwÑhnt wurde.  Desweiteren sollten Bildschirmschoner
    nie die Synchronisations-Art umschalten, da einige Grafikkarten
    mit externer Synchronisation laufen.
    Damit ein Bildschirmschoner auf jeder Grafikkarte lÑuft darf er
    nur VDI- und AES-Funktionen benutzen.
    
Handbuch Abschnitt B-3 (Erweiterte XBios-Funktionen)
----------------------------------------------------
   Die Modi der neuen Xbios-Funktion 'Oscansrc' sind  nicht korrekt
   angegeben worden. Folgende Modi sind richtig:

       Mode  0 -> Setscreen zulaessig
       Mode  1 -> Setscreen NICHT zulÑssig (Default)
       Mode -1 -> Status abfragen

   In der mitgelieferten Datei OVERSCAN.H ist dieser Fehler bereits
   behoben.

==========================================================================
                     Die neuen Programme
                     -------------------

Hardcopy-Treiber
----------------
   Die Hardcopy-Treiber sind nicht mehr im OVERSCAN.PRG integriert,
   sondern werden getrennt installiert.  Dies hat den Vorteil,  daû
   das OVERSCAN.PRG kleiner geworden ist.  Auûerdem brauchen Sie im
   allgemeinen nur einen der Treiber zu installieren,  was wiederum
   Speicherplatz spart.  Die drei mitgelieferten Treiber kînnen Sie
   entweder im Auto-Ornder installieren oder direkt vom Desktop aus
   starten.

   OVER_IMG.PRG
   ------------
     Dieser Hardcopy-Treiber speichert den Bildschirm im  Bit-
     Image-Format in einer SCREEN_x.IMG und SCREEN_x.GEM Datei
     auf dem Startlaufwerk ab.  Das Image- bzw. GEM-Format ist
     das ATARI-Standardformat fÅr Bilddateien. Sie kînnen dann
     diese Dateien mit OUTPUT.PRG ausgeben,  mit EASYDRAW oder
     einem anderen Zeichenprogrammm verÑndern oder sie auch in
     1St-Wordplus einlesen.
     Das Programm benîtigt nach der Installation etwa  1K Byte
     Speicherplatz und verwendet die XBRA-Kennung OVIM um sich
     im Hardcopy-Vektor zu installieren. Die BitImage-Hardcopy 
     wird nur bei Monochrom-Bildschirm unterstÅtzt.

   OVER_EPS.PRG
   ------------
     Dies ist der Treiber fÅr EPSON-kompatible Drucker. Da auf
     die im Betriebssystem vorhandenen Druckerroutinen zurÅck-
     gegriffen wird, funktioniert das Programm  auch in Farbe,
     es findet dann eine Graustufen-Umwandlung der Bildschirm-
     farben statt. Durch das Betriebsystem werden nur Ausgaben  
     bis 640 Pixeln Breite unterstÅtzt, zusÑtzliche Pixel wer-
     den leider abgeschnitten.
     Nach der Installation werden nur 500 Bytes belegt, da auf
     auf Systemroutinen zurÅckgegriffen wird.  Der Treiber be-
     nutzt die XBRA-Kennung OVEP um sich  in den Hardcopy-Vek-
     tor einzuhÑngen.

   OVER_24N.PRG (ab Version 2.0 von Patrick Dubbrow)
   ------------
     Dieser Treiber ist fÅr 24-Nadeldrucker gedacht.  Damit er
     mîglichst  viele  Drucker  unterstÅtzt,  kînnen Sie  ihn,
     falls  notwendig, mit  einem Diskmonitor an Ihren Drucker
     anpassen.

     Auf der OverScan-Diskette ist der Treiber fÅr NEC-Drucker
     und dazu kompatible konfiguriert.

     Hinter dem String 'OverScan' befinden sich 4 mal 8 Bytes,
     die die Druckerkomandos beschreiben:

        Default-Werte         |  Bedeutung
     -------------------------+----------------------------
     1B 2A 27 80 81 FF 00 00 | Init fÅr 180x180 Dpi Modus
     1B 33 18 FF 00 00 00 00 | Zeilenabstand: 24/180 Inch
     1B 40 FF 00 00 00 00 00 | Druckerreset am Ende
     0D 0A FF 00 00 00 00 00 | Zeilenvorschub nach Zeile


     Alle Werte sind hexadezimal, der Wert $FF kenzeichnet das
     Ende des jeweiligen  Befehls.  Bei den Init-Strings steht
     der Wert $80 fÅr das Low-  und der Wert $81 fÅr das High-
     Byte der auszugebenden Breite, die  wirklichen Werte wer-
     den vom Treiber eingesetzt.  $80 und $81 sollen  also nur
     die Position innerhalb des Init-Strings kennzeichnen. Die
     Null-Bytes am Ende jeder Zeile sind unbenutzt.

     Es folgt das  SchlÅsselwort  'PINI'.  Wie  alle OverScan-
     Hardcopy-Treiber   unterstÅtzt   auch  OVER_24N.PRG   den
     Hardware-Reset Ihres  an der parallelen Schnittstelle an-
     geschlossenen Druckers, wenn Sie die kleine Schaltung aus
     dem  Artikel  "PINI  -  so sag ich's meinem Drucker"  von
     Patrick Dubbrow im ST-Magazin 6/91 in Ihren Atari ST oder
     TT eingebaut haben.  Falls nicht, hat  die  Konfiguration
     des Hardcopy-Treiber auf dessen FunktionsfÑhigkeit keinen
     Einfluû.
     Wenn das Byte hinter  dem  String "PINI" den Wert 1  hat,

     wird vor jeder Hardcopy ein  Printer-Reset  durchgefÅhrt,
     um optimale Fehlervermeidung zu gewÑhrleisten. Hat es den
     Wert 0, geschieht nichts derartiges.

     Die QualitÑt des Ausdrucks kînnen  Sie mit dem ATARI-Kon-
     trollfeld  zwischen 'Test' = groûe Hardcopy und 'Maximum'
     = kleine Hardcopy umschalten.  Das Programm benîtigt nach
     der Installation etwa 1 Kbyte Speicherplatz und verwendet
     die  XBRA-Kennung OV24 um sich im  Hardcopy-Vektor zu in-
     stallieren. Der 24-Nadel-Treiber wird nur beim Monochrom-
     Bildschirm  unterstÅtzt, installiert  sich aber wegen der
     mîglichen Auflîsungsumschaltung auch in Farbmodi.

   OVER_DJT.PRG
   ------------
     Dieser Treiber ist fÅr  Drucker gedacht,  die Åber ASCII-
     Kommandos gesteuert werden. Damit er mîglichst viele Dru-
     cker unterstÅtzt, kînnen Sie ihn mit einem Diskmonitor an
     Ihren Drucker  anpassen.  

     Auf der OverScan-Diskette ist der Treiber fÅr HP Diskjet+
     oder Ñltere LaserJet-Drucker konfiguriert.

     Hinter dem String 'OverScan' finden  Sie 10 mal 10 Bytes,
     die die Druckerkomandos beschreiben.

        Default-Werte         |  ASCII     |Bedeutung
     -------------------------+------------+------------------
      1B 2A 72 42 00 00 00 00 | ESC *rB    | Rastergraphik aus
      1B 2A 74 33 30 30 52 00 | ESC *t300R | 300 DPI	 
      1B 2A 74 31 35 30 52 00 | ESC *t150R | 150 DPI
      1B 2A 72 80 80 80 53 00 | ESC *r???S | Pixel pro Zeile
      1B 2A 62 30 4D 00 00 00 | ESC *b0M   | Non-runlength mode
      1B 2A 72 31 51 00 00 00 | ESC *r1Q   | Draft-Quality
      1B 2A 72 32 51 00 00 00 | ESC *r1Q   | Letter-Quality
      1B 2A 72 30 41 00 00 00 | ESC *r0A   | Linker Rand 
     -------------------------+------------+------------------
      1B 2A 62 90 90 57 00 00 | ESC *b??W  | Bytes pro Zeile 
     -------------------------+------------+------------------
      1B 2A 72 42 0D 0A 00 00 | ESC *rB CR | Ende

     Alle Werte sind hexadezimal,  die $00 Werte am Ende einer
     Zeile werden nicht ausgegeben. Die $80-Werte bei der Zei-
     le in der die 'Pixel pro Zeile' gesetzt werden,  wird vom 
     der wirkliche Wert erst durch den Treiber eingesetzt. Bei
     640 Pixeln  steht dann dort  der String  'ESC *r640S'. In 
     der Zeile 'Bytes pro Zeile' werden die $90-Werte erst zur
     Laufzeit durch die Anzahl der Bytes pro Zeile ersetzt. Im
     Normalmodus  wird  also der String  'ESC *b80W' am Anfang 
     jeder Pixelzeile gesendet.
     Die  der aktuellen Einstellung im Kontrollfeld entsprech-
     enden Strings im oberen  Feld werden vor dem  Ausdruck an
     den Drucker gesendet. FÅr jede Druckzeile wird der String 
     im mittleren Feld gefolgt von den  Pixeldaten Åbertragen.
     Am Ende des Ausdrucks wird die Ende-Zeile Åbertragen.
     
     Mit dem ATARI-Kontrollfeld  kînnen Sie  verschiedene Ein-
     stellungen vornehmen.

           Kontrollfeld    |     Druckertreiber
       --------------------+--------------------------
       1280    / 960 Pixel |  300-DPI  / 150-DPI
       Test    / Maximum   |  Draft-   / Letter-Quality
       Printer / Modem     |  Printer- / Modem-Ausgabe
       
     Das Programm benîtigt nach der Installation etwa  1K Byte
     Speicherplatz und verwendet die XBRA-Kennung OVDJ um sich
     im Hardcopy-Vektor zu installieren.  Der  DiskJet-Treiber
     wird nur beim Monochrom-Bildschirm unterstÅtzt.
        
     
   Es ist immer der zuletzt gestartete Treiber aktiv. Wenn Sie z.B.
   zuerst den 24N-Treiber starten,  dann aber eine  IMAGE-Kopie des
   Bildschirms benîtigen, so  mÅssen Sie nur  den  OVER_IMG-Treiber 
   starten. Um den 24N-Treiber wieder zu aktivieren, mÅssen Sie das
   OVER_24N.PRG erneut  starten.  Der Treiber  stellt fest,  daû er 
   schon im Speicher vorhanden  ist  und der  zuerst  geladene 24N-
   Treiber wird reaktiviert. Somit geht durch das Reaktivieren kein
   neuer Speicherplatz verloren.
   
   OVER_LAS.ACC
   ------------
     Dieses Accessorie ist ein Patch fÅr den DMC-LaserBrain Treiber
     fÅr den ATARI SLM 804 Laserdrucker.  Das DMC-Acessorie (in der 
     Version 1.33 und kleiner) lÑuft leider nicht mit OverScan oder
     Groûbildschirmen  zusammen, da es  direkt  in  den Bildschirm-
     speicher schreibt.
     Das OVER_LAS.ACC lÑdt nun den DMC-Treiber nach  und immer wenn
     dieser angesprochen wird (Sei es Åber die MenÅleiste oder Åber
     Alternate-Help) wird in den Normalmodus umgeschaltet. Wenn der
     DMC-Treiber seine Arbeit beendet hat, wird der Bildschirm kor-
     rekt restauriert und auf den vorherigen Modus eingestellt.

     Kopieren Sie dazu das OVER_LAS.ACC auf Ihre  Bootdiskette, das
     DMC-LaserBrain Accessorie mÅssen  Sie in LASBRAIN.IMG umbenen-
     nen.  Sie kînnen den DMC-Treiber aber auch in den FONTS-Ordner
     zu den Fonts kopieren und den kompletten Pfad in den Patchvar-
     iablen vom  OVER_LAS.ACC angeben. Mit einem DiskMonitor mÅssen
     Sie nur den String 'PathName'  suchen, gleich dahinter beginnt
     der Filename. Das LASBRAIN.BAT Konfigurationsfile muû Åbrigens
     im selben Pfad liegen, wie das LASBRAIN.IMG.
     
     Da der DMC-Treiber nur im Monochrom-Modus  funktioniert,  wird 
     er im Color-Modus nicht nachgeladen. Sie kînnen das Laden ver-
     hintern, indem Sie beim Starten des Accessories die Alternate-
     Taste  gedrÅckt halten. Wurde  das LASBRAIN.IMG nicht geladen,
     so wird nur eine Info-Box ausgegeben. 

     Das  OVER_LAS.ACC  benîtigt insgesamt 11 KB Speicher, als Code
     und um den Bildschirm  restaurieren zu kînnen.  Es benutzt die
     XBRA-Kennung OVLB um sich im GEM-Trap, BIOS-Trap und dem HARD-
     COPY-Vektor zu installieren.

OVER_FSB.PRG
------------
   Dieses Auto-Ordner Programm ist ein  Patch fÅr FileSelctorBoxen.
   Es  gibt  einige  PD-FileSelctorBoxen (z.B. von Martin Pratzel),
   die noch nicht  AutoSwitch- oder  gar OverScan-fÑhig  sind.  Das
   OVER_FSB.PRG lÑdt das andere FileSelctor-Programm nach. Wird nun
   die  FileSelctorBox  angefordert, so  schaltet  OVER_FSB  in die
   eingetragene Auflîsung um und  stellt dann  die Box dar. Ist die
   Box beendet, wird  auf den vorherigen Modus umgeschaltet und der
   Bildschirm restauriert. 
   Mit Hilfe eines  Disk-Monitors  kînnen Sie das OVER_FSB-Programm
   an die  FileSelectorBox  anpassen.  Hinter dem String 'PathName'
   ist  der  Pfad  des  nachzuladenden Programms  eingetragen,  als
   Default ist 'FSELECT.IMG' vorgegeben, daû  sich nicht  im  Auto-
   Ordner, sondern auf der Root befinden muû.
   Vor dem String  'PathName' kînnen Sie den  Modus angeben, in den
   beim Aufruf der Box umgeschaltet werden soll. 

      Wert | Modus
     ------+---------------
       $00 | Normalmodus
       $01 | OverScan
       $FF | nicht umschalten
   
   Da QuickST 2.1 einige Tricks beim EinhÑngen in den GEM-Trap ver-
   anstaltet, ist auch  der Modus 'nicht umschalten' ganz nÅtzlich.
   Die LittleGreen-FileSelctorBox ist  AutoSwitch-fÑhig. Nur  unter
   QuickST 2.1 wird sie beim Umschalten in den Normalmodus zusammen
   mit QuickST 2.1 ausgehÑngt.  LÑdt man die LG-Select-Box hingegen
   mit OVER_FSB, so  tritt dieses  PhÑnomen nicht auf,  da OVER_FSB
   speziell auf die Tricks von QuickST 2.1 vorbereitet ist.

   Getestet wurde  OVER_FSB bisher nur mit der LG-SelectBox, UIS-II
   und der PD-FSELECT-Box vom Martin Pratzel. Der Patch sollte aber
   auch  mit  anderen  Auto-Ordner FileSelctorBoxen  funktionieren.

   Wenn Sie beim Starten die Alternate-Taste  gedrÅckt halten, wird
   die FileSelectorBox nicht nachgeladen.

   Das OVER_FSB.PRG benîtigt insgesamt 11 KB (Im Color-Modus 20 KB)
   Speicher, als Code und um den Bildschirm restaurieren zu kînnen.
   Es benutzt die XBRA-Kennung OVFS um sich im  GEM-Trap, BIOS-Trap
   und dem EXEC-OS-Vektor zu installieren.  Unter QuickST  wird der
   BIOS-Trap  Åbrigens  auf 2  unterschiedliche Routinen umgehÑngt.

   Das OVER_FSB.PRG muû hinter  OVERSCAN,  aber vor  QuickST 2.1 im 
   Auto-Ordner ausgefÅhrt werden.

OVER_WIN.PRG
------------
   Das OVER_WIN.PRG ist ein Patch  fÅr Programme,  die ihre Fenster
   zu groû îffnen. Das beste  Beispiel ist CALAMUS: Jedesmal,  wenn
   man ein  neues Dokument îffnet,  wird das  Fenster viel zu breit
   geîffnet. Ein anderes Beispiel wÑre TurboC:  In der TC.INF-Datei
   wird die Position  und  Grîûe der  geîffneten  Fenster vermerkt. 
   Startet man  nun  TurboC im Normalmodus, so  liegen die Fenster-
   elemente alle auûerhalb des Bildschirms -- Man  kann die Fenster
   also auch nicht verkleinern.
   
   Das OVER_WIN.PRG reagiert, wenn ein Programm ein Fenster îffnet,
   so daû es auûerhalb des Bildschirms liegt. Es gibt drei mîgliche
   Einstellungen:
   
   Outside-Modus
   -------------
     Im Outside Modus  reagiert  das  OVER_WIN jedesmal,  wenn  ein 
     Programm versucht ein Fenster so zu îffnen,  daû es  von vorne
     herein auûerhalb des Bildschirmbereichs liegt.	
     
   Calamus-Modus
   -------------
     In diesem Modus  reagiert  OVER_WIN  nur,  wenn  das  Programm 
     CALAMUS ein neues Fenster îffnet, oder wenn ein Fenster so ge-
     îffnet  wird,  daû  Fensterelemente  selbst  durch Verschieben 
     nicht erreicht werden kînnen.
     
   Too big-Modus
   -------------
     OVER_WIN  reagiert  nur,  wenn  ein  Programm  ein  Fenster so
     îffnet,  daû  die Fensterelemente selbst durch Verschieben des
     Fenster nicht erreicht werden kînnen.
   
   Der  Defaultmodus  kann  mit  einem Diskmonitor Åber eine Patch-
   variable vorgegeben werden.  Im  OVER_WIN-File  finden  Sie  die
   Zeichenfolge 'DefMode'.  Genau davor  befindet sich der Wert fÅr
   die Defaulteinstellung.
   
        Name     | Patchwert
      -----------+-----------
        Outside  |  1
        Calamus  |  2
        Too big  |  3
     
   Das OVER_WIN.PRG kann auf 3  Arten installiert werden.  Im Auto-
   Ordner, als Accessory und als normales  Programm.  Beim Start im 
   Auto-Ordner  wird  der  eingestellte  Default-Modus benutzt; das
   OVER_WIN.PRG  muû hinter  OverScan, aber  vor  QuickST gestartet
   werden.  Beim Start  vom Desktop  kînnen Sie den Modus in  einer
   Alertbox vorgeben. Wird das  OVER_WIN als Accessory installiert,
   so kînnen Sie den Modus Åber die MenÅleiste wechseln.
   
   Das OVER_WIN.PRG benîtigt 2.5 KB Speicher.  Es benutzt die XBRA-
   Kennung OVWI, um sich im  GEM-Trap,  BIOS-Trap und dem  EXEC-OS-
   Vektor (Nur bei Start aus dem Auto-Ordner) zu installieren.
   
OVER_EXC.PRG
------------
   Da  die  Bomben-Routine des TOS nicht  auflîsungsunabhÑngig ist,
   werden die bei Fehlern auftretende  Bomben  immer im Normalmodus
   gezeichnet. 
   Falls Sie dies  stîrt, kînnen  Sie mit  OVER_EXC.PRG  eine  neue
   Routine installieren, die auflîsungsunabhÑngig arbeitet. 

   Wenn Sie beim Starten die Alternate-Taste  gedrÅckt halten, wird
   der Patch nicht installiert.
   
   Das OVER_EXC.PRG benîtigt 500 Byte Speicher und installiert sich
   mit der  XBRA-Kennung OVEX in allen Exception-Vektoren, die  auf 
   auf den Orginal-Exception-Handler zeigen.

OVERUTIL.ACC
------------
   Das Accessorie bietet einen autoswitch-fÑhigen Bildschirmschoner
   und eine autoswitch-fÑhige Desktop-Uhr in der MenÅleiste.  Viele
   neue Bildschirmschoner oder Uhren funktionieren zwar unter Over-
   Scan,  passen sich aber beim automatischen  Umschalten nicht der
   neuen Grîûe des Bildschirms an. 
   In der Dialogbox  kînnen Sie OverUtil.Acc konfigurieren  und die 
   aktuelle Einstellung  mit dem 'Save'-Button im Acc-File sichern.
   
   Bildschirmschoner
   -----------------
     Alle Einstellungen, die den Bildschirmschoner betreffen,  sind
     im 'Screen saver'-Feld zu finden. Den Bildschirmschoner kînnen
     Sie mit dem 'OFF'-Knopf abschalten.  Mit dem '3 Min.'- und dem
     '5 Min.'-Knopf legen Sie die Ansprechzeit fest.

     Es sind zwei verschiedene Methoden im Accesorie enthalten. Die
     erste Methode ist VDI-konform programmiert und lÑuft daher auf
     allen Grafikkarten.  Ist  die gewÅnschte Zeit verstrichen,  so 
     werden die anderen Programme gestoppt (falls dies mîglich ist)
     damit  diese  keine Ausgaben mehr  auf  den  Bildschirm machen 
     kînnen. Nun wird die Bildschirmschoner-Grafik gezeichnet. Wird
     eine Taste gedrÅckt oder die Maus bewegt, so  wird das jeweils
     aktuelle Programm zum Neuzeichnen des Bildschirms aufgefordert.
     Der Speicherbedarf dieser Methode ist minimal, es wird nur der
     Speicher zum Puffern der MenÅleiste benîtigt,  wenn mehr Spei-
     cher  vorhanden  ist, so wird  der ganze Bildschirm gepuffert.
     Diese Methode wir benutzt, wenn der 'VDI only'-Knopf angewÑhlt
     ist.

     Die zweite Methode lÑuft nicht  mit  Groûbildschirmen,  da auf
     eine zweite Bildschirmseite umgeschaltet wird. Es muû auûerdem
     genÅgend  Speicher fÅr die 2. Seite vorhanden sein,  ansonsten 
     wird die 'VDI only'-Methode verwendet. Durch die 2.Bildschirm-
     seite mÅssen  die  anderen Programme nicht angehalten  werden,
     denn diese  benutzen  den 'Orginal'-Bildschirmspeicher. Darge-
     stellt  wird  hingegen  die  Bildschirmseite  des  Bildschirm-
     schoners.  Der Bildschirmschoner benîtigt nur 1-2% Rechenzeit,
     der  Rest  steht  voll den  anderen Programmen  zur VerfÅgung. 
     Wenn das aktuelle Programm keine AES-Aufrufe macht, bleibt die
     Bildschirmgrafik stehen.  Dies ist immer der  Fall,  wenn TTP- 
     oder TOS-Programme nachgestartet werden. Je mehr AktivitÑt das
     Hauptprogramm  und die  anderen Accessories entwickeln,  um so
     ruckeliger  lÑuft die Ausgabe der Grafik.  Diese Methode  wird
     benutzt, wenn der '2.Screen'-Knopf angewÑhlt ist  und genÅgend 
     Speicher vom aktuellen Programm Åbriggelassen wurde. Auf Groû-
     bildschirmen wird automatisch die 'VDI-only' Methode gewÑhlt.

     Wenn Sie Åber genÅgend Speicher  verfÅgen,  kînnen Sie mit dem
     'M'-Knopf den  notwendigen  Speicher  gleich beim Start reser-
     vieren lassen. Diese Einstellung wird erst beim nÑchsten Start
     Åbenommen.

     Mit dem 'Blank'-Knopf  kînnen  Sie eine sofortige Dunkelschal-
     tung veranlassen.

     Wenn Sie den Mauszeiger in der linken unteren  Ecke abstellen,
     wird der Bildschirmschoner  aktiviert.  Stellen  Sie den Maus-
     zeiger hingegen in die rechte untere Ecke,  so ist der Schoner
     ausgeschaltet.

   Desktop-Uhr
   -----------
     Alle  Einstellungen,  die die Desktop-Uhr  betreffen,  sind im
     'Clock'-Feld zu finden. Mit dem 'OFF'-Knopf kînnen Sie die Uhr
     abschalten.
         
   Desktop-Verschieben
   -------------------
      Der GEM-Desktop  kann mit den beiden Pfeil-Knîpfen horizontal 
      verschoben werden. Dies ist fÅr Besitzer von Monitoren inter-
      essant, deren  Bildschirm im klaten Zustand des Monitors eine 
      andere age hat, als im warmen Zustand.
      
   Das Accessorie benutzt die XBRA-Kennung OVUT um sich im IKBDSYS-
   und Mouse-Vektor zu installieren. 
   
OVERDEMO.PRG
------------
   Es wird ein neues Demo 'OVERDEMO.PRG' mitgeliefert.  Dieses Demo
   zeigt sehr deutlich, welchen Zugewinn an Auflîsung man durch den 
   Einbau von AutoSwitch-OverScan erhalten hat und wieviel Speicher 
   dafÅr benîtigt wird. Die Versionsnummer 0.4 deutet schon an, daû
   weitreichende Tests noch nicht erfolgt sind.
   Bei einem Umschalten von Normalmodus auf OverScan-Modus kann von
   OverScan nur der alte Anteil des Bildschirms restauriert werden.
   Wird die Auflîsung beim Starten/Enden von Programmen gewechselt,
   so wird das 'Neuzeichnen' des Desktop und der MenÅleiste vom GEM
   erledigt. Bei einem Programm,  daû wÑhrend der Laufzeit die Auf-
   lîsung wechselt,  muû dieses Neuzeichnen vom Programm selbst er-
   ledigt werden. 

Neuer Abschnitt Reihenfolge im AutoOrdner
----------------------------------------- 
   Die Reihenfolge im Auto-Ordner  stellt fÅr  viele Programme  ein
   Problem dar. 
   
     OverScan <> GDOS    
       Der OverScan-Treiber sollte vor GDOS gestartet werden,  wenn
       Sie den  NDC-Modus  benutzen mîchten.  Da  dieser Modus aber
       noch von keinem bekannten Programm unterstÅtzt wird, ist die
       Reihenfolge egal.
       
     OverScan <> QuickST 
       Damit QuickSt die  neue Auflîsung unterstÅtzt,  muû es  nach
       OverScan gestartet werden.  Auûerdem sollte  QuickST 2.1 als 
       letztes Programm im Auto-Ordner laufen.
       
     OverScan <> Over_FSB
              <> FileSelctorBoxen
       Alle Patches,  die etwas  mit dem Bildschirm zu  tun  haben,
       sollten hinter dem OverScan-Treiber gestartet  werden.  Wenn
       sich das  Programm  in  den  GEM-Trap  einhÑngt,  muû es vor
       QuickST gestartet werden.                               
     
   Soweit mîglich, das OverScan.Prg  als  erstes  Programm im Auto-
   Ordner  installieren.  TOS-Patches / Plattentreiber / Cache  und
   Fatspeed-Programme kînnen auch vor OverScan gestartet werden.
   
